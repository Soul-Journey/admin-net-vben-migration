# Admin.NET WebUI 迁移到 Vben 执行总控台

最后更新：2026-07-29

这个文件是当前工作区里 Admin.NET WebUI 迁移的默认执行依据。以后用户说“继续计划”“下一步”“现在到哪了”“改一下计划”，都优先看这个文件，而不是依赖聊天记录。

## 工作规则

- 旧前端 `E:\admin.NET\Web` 永久只读，只允许运行和对照，禁止任何代码、配置和依赖修改。
- 新前端统一放在 `E:\admin.NET\vben-web`，基于 Vben 5 的 `apps/web-antd`。
- 默认优先适配现有 Admin.NET 后端接口，不轻易改后端协议。
- 如果必须改后端，改动要小、明确，并同步写入本文件。
- 每完成一个阶段或关键任务，都更新本文件的进度和“下一步默认任务”。
- 如果用户改变方向，先更新“关键决策”和“下一步默认任务”，再继续写代码。

## 当前状态

整体进度：当前数据库启用的 37 个内部路由均已有真实页面或明确能力边界；核心账号、角色、菜单、机构、职位、字典、租户写链路已经过真实接口与数据库验收。功能迁移主体已完成，剩余工作是外部服务、文件生成和高风险操作的受控人工验收，不再新增旧版不存在的菜单或功能。

已完成：

- Admin.NET 后端已运行在 `http://localhost:5005`。
- 旧版 Admin.NET Web 已运行在 `http://localhost:8888`。
- 新版 Vben Web 已运行在 `http://localhost:5666`。
- 后端数据库已切换到 MySQL：
  - 地址：`localhost`
  - 端口：`3306`
  - 用户：`root`
  - 密码：由本地运行配置注入，不在计划文档和日志中保存明文
  - 数据库：`AdminNET`
- 项目本地 Node 和 pnpm 已安装在 `E:\admin.NET\vben-web\.runtime`。
- Vben 请求层已适配 Admin.NET 返回结构：
  - 成功码：`code === 200`
  - 数据字段：`result`
  - Token 响应头：`access-token`、`x-access-token`
- Vben 账号密码登录已接入 Admin.NET：
  - `POST /api/sysAuth/login`
  - 支持 SM2 密码加密。
  - 页面上已隐藏租户输入框。
  - 内部仍默认提交 `tenantId = -1`。
- Vben 手机号登录已接入 Admin.NET：
  - 发送短信验证码：`POST /api/sysSms/sendSms/{phoneNumber}`
  - 手机号登录：`POST /api/sysAuth/loginPhone`
  - 实际能否收到短信取决于后端 `SMS.json` 是否配置真实短信服务商。
- Vben 用户信息、按钮权限、后端菜单已接入：
  - `GET /api/sysAuth/userInfo`
  - `GET /api/sysMenu/ownBtnPermList`
  - `GET /api/sysMenu/loginMenuTree`
- Vben 动态菜单已经和旧版 `8888` 菜单基本一致。
- 未迁移页面会进入迁移占位页，不再 404 或白屏。
- 微信、QQ、GitHub、Google、扫码登录、注册入口已暂时隐藏，等 OAuth 和账号绑定方案确定后再开启。
- `system/user` 已进入新的 Vben 用户管理页，不再进入迁移占位页。
- 用户管理页已接入真实后端用户列表、搜索、机构树、角色/职位选项、状态切换、删除、复制、重置密码、解除锁定等第一版能力。
- `system/role` 已进入新的 Vben 角色管理页，不再进入迁移占位页。
- 角色管理页已接入真实后端角色列表、租户筛选、搜索、状态切换、删除、修改记录、角色新增/编辑、菜单授权、数据范围授权等第一版能力。
- `system/menu` 已进入新的 Vben 菜单管理页，不再进入迁移占位页。
- 菜单管理页已接入真实后端菜单树、租户筛选、菜单名称/类型搜索、树表展开折叠、刷新、新增/编辑/复制/删除、修改记录等第一版能力。
- `system/dict` 已进入新的 Vben 字典管理页，不再进入迁移占位页。
- 字典管理页已接入真实后端字典类型和字典值分页、左右联动、搜索、新增/编辑/复制/删除、系统内置保护、修改记录等第一版能力。
- 字典值新增/编辑的标签类型已改为彩色单选标签，界面显示中文语义，保存值仍兼容后端 `primary/success/info/warning/danger`。
- 字典类型、字典值、机构这类短表单弹窗已取消不必要的内层滚动条，内容自然撑开显示。
- `system/org` 已进入新的 Vben 机构管理页，不再进入迁移占位页。
- 机构管理页已接入真实后端机构树、左侧租户选择、左侧机构导航、右侧树表、机构类型字典、搜索、展开折叠、刷新、新增/编辑/复制/删除、修改记录等第一版能力。
- 机构管理修改记录已从黑底 Tooltip 改为白色 Popover，与账号、字典页细节保持一致。
- `system/pos` 已进入新的 Vben 职位管理页，不再进入迁移占位页。
- 职位管理页已接入真实后端职位列表、租户筛选、职位名称/编码搜索、新增/编辑/复制/删除、在职人数、人员明细、状态、修改记录等第一版能力。
- `system/tenant` 已进入新的 Vben 租户管理页，不再进入迁移占位页。
- 租户管理页已接入真实后端租户分页、租户名称/电话搜索、新增/编辑、Logo Base64 上传、启用注册、默认注册方案、状态切换、创建库、进入租管端、切换租户、授权菜单、同步授权、重置密码、删除租户等第一版能力。
- 租户管理高风险动作已统一放入二次确认：创建库、进入租管端、切换租户、同步授权、重置密码、删除租户。
- 租户管理三个点菜单的六个动作已补齐图标、二次确认、真实接口调用、成功提示和防重复执行状态。
- 租户管理“进入租管端”和“切换租户”已明确提示身份差异：进入租管端会切到该租户管理员账号，切换租户会保留当前用户身份，二者菜单权限可能不同。
- 租户授权菜单已改为按被授权租户 ID 加载菜单树，避免切换登录上下文后用错当前账号菜单范围。
- 租户授权菜单树已按旧版习惯改成横向紧凑布局：菜单显示路径标签，按钮权限横向换行，只显示中文动作名。
- 租户编辑弹窗已补回旧版“从库连接串”格式提示，避免用户不知道 JSON 权重连接串怎么填。
- 退出登录后的登录页已关闭右侧登录壳层的进入/浮动动画，并修复登录页 redirect 套娃导致 URL 越滚越长、页面反复闪动的问题。
- 登录成功后的跳转已改为保存 Token 和用户信息后直接进入 `/dashboard/home`，不再依赖登录页残留的 redirect 状态，避免第一次登录停留在登录页。
- 登录页登录和后台“登录过期续登弹窗”必须分开处理：登录页即使残留 `loginExpired` 也要进入工作台；续登弹窗只关闭弹窗、不改变当前业务页面。
- Vben 开发环境已补齐 Admin.NET SM2 登录公钥，重启后账号密码登录仍可正常对接后端。
- `platform/config` 参数配置已迁移为 Vben 页面：支持名称、编码、分组查询，新增、编辑、单条/批量删除、布尔值中文显示、修改记录和内置参数保护。
- 参数配置接口已按租户覆盖值重新梳理：普通租户只能修改自己的覆盖值，只有超级管理员可维护全局定义；默认密码等敏感值只返回脱敏标记，不再下发明文。
- 参数配置已完成真实 CRUD 验收：临时参数新增、租户值编辑、删除均成功，数据库复核主表和覆盖表无测试残留。
- 参数配置已补齐旧版表格自带的“导出本页 / 导出全部”能力，保留当前搜索条件并导出 Excel 可直接打开的 UTF-8 CSV；实现不引入旧版表格解析依赖，并防止以 `= + - @` 开头的内容触发公式注入。
- 请求解包、Token 响应头同步和菜单适配器均已有单元测试；核心 CRUD 验收脚本不保存账号密码，只从当前会话环境变量读取测试凭据。
- 字典值分页的“显示文本”筛选已修复为查询 `Label`，字典值状态切换后会按正确的 `DictTypeId` 清理缓存。

## 关键决策

- 迁移策略：用 Vben 重建新前端，不在旧 Element Plus Web 上做大改。
- UI 方向：Vben 5 + Ant Design Vue。
- 后端接口：尽量保持 Admin.NET 原接口不变，由新前端做适配。
- 租户登录逻辑：
  - `tenantId = -1` 或空值代表默认租户。
  - 正数 `tenantId` 代表指定租户。
  - 租户不存在或被禁用时，由后端拒绝登录。
  - 租户的 ID 隔离、数据库隔离是数据隔离类型，不是登录状态。
- 登录 UI：
  - 账号密码登录作为主入口。
  - 手机号登录保留并接入后端。
  - 第三方登录、扫码登录、注册以后再做。
- 视觉方向：
  - 高密度、克制、清爽、可扫描。
  - 后台业务页不要做成营销页。
  - 列表页优先保证搜索、批量操作、列设置、密度、权限按钮好用。
  - 左侧树和权限树要清爽、有层级图标、hover/选中态舒适，不保留无意义数字徽标。
  - 三点工具菜单点击后必须自动关闭，刷新动作必须有加载态或成功反馈。
  - 登录页、弹窗、表单区域禁止出现无业务意义的持续闪动动画；后台系统优先稳定、清晰、可信。
  - 角色管理这类中等复杂表单优先使用居中弹出层；侧边栏只用于更适合右侧连续查看的详情或编辑场景。
  - 权限树默认不要全部展开；应提供搜索、展开、折叠、刷新和已选数量反馈，避免菜单/按钮权限过多时拥挤难找。
  - 修改记录/详情类内容使用白色 Popover，不用黑底 Tooltip 承载复杂内容；表格里只放轻量图标按钮，不暴露大段“详情”文字。
  - 如果页面有独立“修改记录”列，入口保持和角色管理一致：信息图标 + “详情”文字；字典管理这类列宽紧张页面才收进固定操作列并图标化。
  - 表格操作按钮必须阻止行选中冒泡；独立“修改记录”列可用 hover 触发，若详情入口收进操作列则用 click 触发，避免浮层遮挡同一行编辑/删除按钮。
  - 列较多的左右分栏表格要优先合并低频列到操作区，减少横向滚动；操作列保持固定可见。
  - 新版列表分页统一复用 `ADMIN_PAGINATION_PROPS`：提供 `10/20/50/100` 每页选项、数据总数、每页条数切换和页码快速跳转；只有一页时可由 Ant Design Vue 自动隐藏无意义的跳页输入框，禁止页面自行重复定义分页选项。
  - 新增按钮优先放在查询工具区，同一类 CRUD 页面的按钮位置、弹窗宽度、底部按钮区要保持统一，避免页面显得臃肿。
  - 短表单弹窗不要为了统一高度强行加内层竖向滚动条；能在当前视口自然放下就直接展示。确实超长时优先用标签页、分组、步骤或更合理的弹窗宽度解决。
  - 字典、枚举、状态、标签类型等选项不能只暴露英文值；表单里优先显示中文语义、颜色预览或示例标签，保存仍使用后端需要的编码。
  - 连接串、JSON 配置、扩展数据这类容易填错的字段，要保留旧版提示、格式示例或说明文本，不能只给空输入框。
  - 高风险或会切换上下文的动作必须有二次确认、执行中状态、防重复点击和成功/失败反馈。
  - 后续页面默认带入账号管理的细节标准：控件密度统一、动作反馈明确、旧版功能不漏、视觉不要显得粗糙。

## 阶段清单

### 第 1 阶段：基线和运行环境

状态：已完成。

- [x] 安装项目本地 Node 22。
- [x] 安装项目本地 pnpm 11。
- [x] Vben 使用项目本地 Node，不依赖系统全局 Node。
- [x] 启动后端。
- [x] 启动旧 Web。
- [x] 启动 Vben Web。
- [x] 后端数据库切换到 MySQL。
- [x] 保留旧 Web 作为对照和回退。

### 第 2 阶段：Vben 基础骨架

状态：已完成。

- [x] 引入 Vben 5 `web-antd` 工作区。
- [x] 配置开发端口 `5666`。
- [x] 配置代理到 Admin.NET 后端。
- [x] 增加 Admin.NET 请求适配层。
- [x] 增加 Token 响应头持久化。
- [x] 增加刷新 Token 请求头支持。
- [x] 增加后端动态菜单组件白名单。
- [x] 增加未迁移页面占位页。

### 第 3 阶段：登录、用户信息、权限、菜单

状态：第一轮已完成。

- [x] 账号密码登录。
- [x] 退出登录。
- [x] 用户信息。
- [x] 按钮权限码。
- [x] 后端菜单树。
- [x] 动态路由转换。
- [x] 首页 `/dashboard/home` 映射。
- [x] 隐藏可见租户输入框，内部默认提交租户。
- [x] 保留并接入手机号登录。
- [x] 隐藏暂缓的第三方登录入口。

待加固：

- [x] 请求解包已有单元测试，覆盖成功解包、非信封响应透传和业务错误抛出。
- [x] Token 响应头同步已有单元测试，覆盖双令牌更新、响应头大小写兼容和失效令牌清理。
- [x] 给菜单转换器加边界用例测试（当前 22 项用例）。
- [ ] 决定生产环境是否需要暴露租户切换。
- [ ] 决定 Vben 版本是否长期关闭后端图片验证码。

### 第 4 阶段：第一个业务标杆页

状态：已完成，后续作为 CRUD 页面基准继续复用。

默认目标：用户管理。

目标：

- 做出 Vben/Ant Design Vue 版本的用户管理页。
- 把它作为后续 CRUD 页面复用范式。
- 验证表格、搜索表单、弹窗或抽屉表单、组织树、租户过滤、树工具菜单、角色分配、档案信息、修改记录、状态切换、重置密码、导入导出、按钮权限。

预期交付：

- [x] `system/user` 路由进入新的 Vben 用户管理页，而不是迁移占位页。
- [x] 页面使用 Vben/Ant Design Vue 组件。
- [x] 页面调用现有 Admin.NET 用户相关接口。
- [x] 用户列表和搜索区接入真实数据。
- [x] 左侧机构树已对齐旧版，显示“系统默认、市场部、开发部、售后部、其他”。
- [x] 按钮级权限生效。
- [x] 旧 Web 不改，继续作为对照。
- [x] 机构树补齐旧版能力：租户下拉、机构搜索、全部展开、全部折叠、根节点、刷新。
- [x] 表格补齐旧版“修改记录”详情浮层。
- [x] 新增/编辑弹窗补齐三标签：基础信息、角色授权、档案信息。
- [x] 角色授权补齐旧版左右穿梭框交互。
- [x] 档案信息补齐证件、生日、性别、民族、地址、学历、政治面貌、办公电话、紧急联系人、备注等字段。
- [x] 对齐旧版账号类型过滤：表单提供“会员、普通账号、系统管理员”，不提供“超级管理员”创建入口，但已有数据仍可显示该类型。
- [x] 按反馈优化机构树视觉：补齐组织导航头、机构数量、树容器、层级图标、hover、选中态、菜单宽度和节点间距。
- [x] 将机构树三点菜单改为自定义工具菜单，包含全部展开、全部折叠、根节点、刷新。
- [x] 修复机构树工具菜单点击后不关闭的问题，并为刷新增加加载态和成功反馈。
- [x] 移除机构数量徽标，避免右上角数字干扰页面视线。
- [x] 新增、编辑、删除、状态切换、重置密码、解除锁定已完成第一版接入。
- [x] 已核对旧版页面、后端接口和菜单权限：账号管理原本没有批量操作、导入导出、列设置、密度切换，不为迁移页面虚构旧版不存在的能力。
- [x] 左侧机构树已按用户反馈完成租户选择、搜索、展开折叠、根节点、刷新、选中态、层级图标和密度优化；后续仅在发现明确缺陷时调整。

验收标准：

- 从 Vben 菜单打开用户管理。
- 可以搜索用户。
- 可以新增用户。
- 可以编辑用户。
- 新增/编辑表单包含“基础信息 / 角色授权 / 档案信息”。
- 角色授权支持左右穿梭选择。
- 机构树支持租户选择、搜索、展开、折叠、根节点、刷新。
- 表格能查看创建者、创建时间、修改者、修改时间、备注。
- 可以启用、禁用用户。
- 如果后端支持，可以重置密码。
- 如果后端支持，可以删除或批量删除。
- 无权限按钮不会显示或不可操作。

### 第 5 阶段：核心系统页

状态：功能迁移已完成，核心 CRUD 已通过隔离测试数据的真实接口和数据库验收；涉及外部系统或不可逆副作用的动作仍保留人工确认。

建议顺序：

- [x] 角色管理。
  - [x] 角色列表、租户筛选、角色名称/编码搜索、分页。
  - [x] 数据范围字典显示。
  - [x] 状态开关。
  - [x] 修改记录浮层。
  - [x] 新增/编辑角色，按反馈从侧边栏改为居中弹出层。
  - [x] 菜单权限树授权，并按反馈优化为默认只展开根层，提供搜索、展开、折叠、刷新和已选数量。
  - [x] 数据范围授权，支持自定义机构树，并按反馈改为居中弹出层。
  - [x] 使用隔离测试角色验收新增、编辑、菜单授权、数据范围授权和删除，清理后无角色菜单、角色机构重复或孤儿关系。
- [x] 菜单管理。
  - [x] 树表列表、租户筛选、菜单名称/类型搜索。
  - [x] 默认只展开根层，提供展开、折叠、刷新。
  - [x] 显示菜单图标、菜单类型、路由路径、组件路径、权限标识、排序、状态。
  - [x] 修改记录浮层。
  - [x] 新增/编辑菜单，按目录/菜单/按钮动态展示字段。
  - [x] 复制菜单。
  - [x] 删除菜单。
  - [x] 使用隔离测试按钮菜单验收新增、编辑和删除；菜单适配器 22 项测试覆盖动态路由转换，清理后全局菜单仍为 180 条。
- [x] 字典管理。
  - [x] 左右双表：字典类型、字典值。
  - [x] 字典类型搜索、分页、选中联动字典值。
  - [x] 字典值显示文本搜索、分页。
  - [x] 字典类型新增、编辑、删除，保留系统内置和枚举类保护。
  - [x] 字典值新增、编辑、复制、删除。
  - [x] 标签类型、Style、Class、扩展数据字段。
  - [x] 修改记录浮层。
  - [x] 按反馈收敛字典页视觉：详情改白色 Popover 和图标按钮，新增按钮归入查询工具区，弹窗尺寸统一收紧。
  - [x] 按反馈二次优化：修改记录合并到操作列、详情改为点击触发、操作按钮阻止行选中、字典值弹窗强制收窄。
  - [x] 使用隔离测试字典验收类型和值的新增、编辑、复制、删除、分页筛选和缓存读取，清理后无测试字典残留。
- [x] 机构管理。
  - [x] 左侧机构导航树，支持搜索、展开、折叠、刷新。
  - [x] 左侧补齐超管租户选择，切换租户后同步刷新机构树和右侧树表。
  - [x] 右侧机构树表，默认只展开根层。
  - [x] 机构名称、机构类型搜索。
  - [x] 新增/编辑/复制/删除机构。
  - [x] 机构类型从 `org_type` 字典加载。
  - [x] 修改记录浮层。
  - [x] 使用隔离测试机构验收新增、编辑和删除；清理后无机构及关联数据残留，旧版目录保持零改动。
- [x] 职位管理。
  - [x] 职位列表、租户筛选、职位名称/编码搜索。
  - [x] 在职人数和人员明细浮层。
  - [x] 状态显示。
  - [x] 修改记录浮层。
  - [x] 新增/编辑/复制/删除职位。
  - [x] 使用隔离测试职位验收新增、编辑、复制和删除；清理后无测试职位残留。
- [x] 租户管理。
  - [x] 租户分页、租户名称/电话搜索。
  - [x] 新增/编辑租户，包含基本信息和站点信息。
  - [x] Logo 本地选择转 Base64 后提交。
  - [x] 表格显示启用注册状态，编辑弹窗支持默认注册方案选择。
  - [x] 状态切换，默认租户禁用状态开关。
  - [x] 站点信息、连接串和修改记录浮层。
  - [x] 创建库、进入租管端、切换租户、授权菜单、同步授权、重置密码、删除租户。
  - [x] 授权菜单树支持搜索、展开、折叠、刷新和已选数量。
  - [ ] 浏览器手工验收新增、编辑、授权菜单、切换租户、进入租管端、创建库、重置密码、删除租户。

### 第 6 阶段：运维和配置页

状态：当前启用菜单范围内的模块迁移已完成；旧版实际存在的导入导出入口已逐项对齐。

- [x] 日志。
- [x] 文件管理。
- [x] 通知公告。
- [x] 定时任务。
- [x] 系统配置。
- [x] 代码生成。
- [x] 导入导出：操作/异常日志服务端 Excel 导出、参数配置本页/全部 CSV 导出、表单设计 JSON 导入导出、打印设计器导出均已接入；账号等旧版没有导入导出的页面不虚构入口。
- [x] 打印。

### 第 7 阶段：重型和专项模块

状态：当前数据库启用菜单范围内已完成；未启用的演示模块不擅自新增菜单。

每个模块先分类：重写、桥接、iframe、延期。

- [x] 审批流。
- [x] 低代码表单设计器。
- [x] LogicFlow。
- [x] Monaco 编辑器。
- [x] Office 预览：当前菜单没有独立入口，文件管理按实际支持的图片/PDF能力提供预览；不新增旧版未启用菜单。
- [x] MQTT：当前数据库菜单未启用，按“旧版翻新、不新增菜单”的规则归为不适用。
- [x] SignalR。
- [x] ECharts 大屏与任务看板。
- [x] 富文本编辑器。
- [x] 打印设计器。

## 下一步默认任务

功能迁移主体和核心真实写链路已完成，下一步只处理受控副作用验收和发现的问题；旧版入口继续永久只读保留。

下一步按顺序做：

1. [x] 清理 Git 提交范围，排除日志、缓存、便携运行时、数据库备份和敏感配置，建立发布候选回滚分支与提交。
2. [x] 备份 MySQL，在隔离测试租户中创建带统一前缀的机构、角色和账号。
3. [x] 验证租户、机构、角色、账号的新增、编辑、授权、同步、上下文切换和删除回收，并复核权限表没有重复、孤儿或测试残留。
4. [x] 核对 `_app.config.js`、Nginx 的 API/SignalR/上传代理、缓存、生产默认账号和敏感配置边界。
5. [x] 菜单、字典、职位核心写链路已使用统一前缀隔离数据验收；文件上传下载此前已验收，参数 Excel 导出已在浏览器实测。
6. [x] 使用统一测试前缀验收打印模板、代码生成后端 ZIP、表单 JSON 导出和流程定义保存/删除；生成物和数据库临时记录均已清理，旧版文件零改动。

## 风险登记

- Element Plus 和 Ant Design Vue 的表单校验、树、表格、上传、日期选择器、弹窗生命周期 API 不一致。
- 旧 Web 生成的 API 名称不一定符合直觉，需要对照实际接口。
- Admin.NET 在 SQLite 和 MySQL 下的种子数据、配置值可能不同。
- 手机号登录依赖真实短信服务商配置，本地可能只能验证接口调用，不能验证收短信。
- 后端动态组件路径必须继续经过前端白名单，不能直接信任后端路径。
- 租户管理包含高风险动作：创建数据库、切换租户、进入租管端、删除租户、重置密码，必须保留二次确认并避免误触。
- 重型模块可能携带全局 CSS 或深度依赖 Element Plus。
- 旧 Web 和 Vben 依赖并存会增加磁盘占用和构建体积。
- 按钮权限容易遗漏，迁移每个页面都要逐项对照旧 Web。

## 验证命令

Vben 必须使用项目本地 Node：

```powershell
$env:PATH="E:\admin.NET\vben-web\.runtime\node-v22.22.0-win-x64;E:\admin.NET\vben-web\.runtime\npm-global;$env:PATH"
cd E:\admin.NET\vben-web
pnpm dev
pnpm exec eslint "apps/web-antd/src/**/*.{ts,vue}"
pnpm -F @vben/web-antd run typecheck
```

说明：当前工作区里 Vben 全量 typecheck 可能比较慢。日常迭代优先跑定向 lint 和页面验证，大任务交付前再跑更完整检查。

## 快速启动三服务

目标端口：

- 后端：`http://localhost:5005`
- 旧版 Web：`http://localhost:8888`
- 新版 Vben Web：`http://localhost:5666`

启动前先检查端口：

> 注意：下面这一段只负责“检查端口”，不会启动服务。看到 `5666` 没有监听时，需要执行后面的启动命令。

```powershell
Get-NetTCPConnection -LocalPort 5005,8888,5666 -State Listen -ErrorAction SilentlyContinue |
  Select-Object LocalAddress,LocalPort,OwningProcess,@{Name='ProcessName';Expression={(Get-Process -Id $_.OwningProcess -ErrorAction SilentlyContinue).ProcessName}}
```

一键启动三服务：

```powershell
$logDir='E:\admin.NET\.run-logs'
New-Item -ItemType Directory -Force -Path $logDir | Out-Null

$nodePath='E:\admin.NET\vben-web\.runtime\node-v22.22.0-win-x64'
$pnpmPath='E:\admin.NET\vben-web\.runtime\npm-global'

$backendCmd = 'cd /d E:\admin.NET && dotnet run --framework net8.0 --project Admin.NET\Admin.NET.Web.Entry\Admin.NET.Web.Entry.csproj --launch-profile Admin.NET.Web.Entry > E:\admin.NET\.run-logs\backend-5005.log 2>&1'
$oldWebCmd = "set PATH=$nodePath;$pnpmPath;%PATH%&& cd /d E:\admin.NET\Web && npm run dev -- --host 0.0.0.0 --port 8888 --strictPort > E:\admin.NET\.run-logs\old-web-8888.log 2>&1"
$vbenCmd = "set PATH=$nodePath;$pnpmPath;%PATH%&& cd /d E:\admin.NET\vben-web\apps\web-antd && pnpm vite --mode development --host 0.0.0.0 --port 5666 --strictPort > E:\admin.NET\.run-logs\vben-5666.log 2>&1"

Start-Process -FilePath cmd.exe -ArgumentList '/c', $backendCmd -WindowStyle Hidden
Start-Process -FilePath cmd.exe -ArgumentList '/c', $oldWebCmd -WindowStyle Hidden
Start-Process -FilePath cmd.exe -ArgumentList '/c', $vbenCmd -WindowStyle Hidden
```

只单独启动新版 Vben Web：

```powershell
$logDir='E:\admin.NET\.run-logs'
New-Item -ItemType Directory -Force -Path $logDir | Out-Null

$nodePath='E:\admin.NET\vben-web\.runtime\node-v22.22.0-win-x64'
$pnpmPath='E:\admin.NET\vben-web\.runtime\npm-global'
$vbenCmd = "set PATH=$nodePath;$pnpmPath;%PATH%&& cd /d E:\admin.NET\vben-web\apps\web-antd && pnpm vite --mode development --host 0.0.0.0 --port 5666 --strictPort > E:\admin.NET\.run-logs\vben-5666.log 2>&1"

Start-Process -FilePath cmd.exe -ArgumentList '/c', $vbenCmd -WindowStyle Hidden
```

启动后验证：

```powershell
Start-Sleep -Seconds 10
Get-NetTCPConnection -LocalPort 5005,8888,5666 -State Listen -ErrorAction SilentlyContinue |
  Select-Object LocalAddress,LocalPort,OwningProcess,@{Name='ProcessName';Expression={(Get-Process -Id $_.OwningProcess -ErrorAction SilentlyContinue).ProcessName}}

(Invoke-WebRequest -Uri http://localhost:5005 -UseBasicParsing -TimeoutSec 10).StatusCode
(Invoke-WebRequest -Uri http://localhost:8888 -UseBasicParsing -TimeoutSec 10).StatusCode
(Invoke-WebRequest -Uri http://localhost:5666 -UseBasicParsing -TimeoutSec 10).StatusCode
```

查看日志：

```powershell
Get-Content -Tail 80 E:\admin.NET\.run-logs\backend-5005.log
Get-Content -Tail 80 E:\admin.NET\.run-logs\old-web-8888.log
Get-Content -Tail 80 E:\admin.NET\.run-logs\vben-5666.log
```

如果前端错误跑到 `5173/5174`，先停掉当前项目相关 Node 再按固定端口重启：

```powershell
Get-CimInstance Win32_Process |
  Where-Object { $_.Name -in @('node.exe','cmd.exe') -and ($_.CommandLine -like '*E:\admin.NET\Web*' -or $_.CommandLine -like '*E:\admin.NET\vben-web*') } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force -ErrorAction SilentlyContinue }
```

## 服务地址

- 后端：`http://localhost:5005`
- 旧版 Web 对照：`http://localhost:8888`
- 新版 Vben Web：`http://localhost:5666`

## 追加规则：接口与数据安全

- 每次迁移或美化页面，不能只看页面是否好看，还必须确认真实接口是否会正确读写数据，尤其是新增、编辑、删除、授权、同步、切换上下文这类会改变数据库状态的动作。
- 后端鉴权必须按三层验收：全局 JWT 负责确认“是否已登录”，`JwtHandler` 与菜单按钮权限负责确认“是否有权调用该接口”，服务内部的数据范围与身份校验负责确认“能操作哪条数据、哪个租户以及是否仅限超级管理员”。`UseAuthentication()`、`UseAuthorization()` 和前端按钮隐藏都不能互相替代。
- 新增后端接口时默认沿用 `AddJwt(... enableGlobalAuthorize: true)` 的全局保护；确需匿名访问必须明确标注 `[AllowAnonymous]` 并单独评审。高风险接口还要在服务端校验管理员级别、租户边界、目标对象和幂等性，不能只依赖数据库菜单权限。
- 超级管理员专用校验只用于全局配置、任务调度控制、服务器敏感信息、跨租户维护等高风险能力；普通租户 CRUD 应使用按钮权限和租户/对象范围校验，不能为了省事全部限制为超级管理员。
- Admin.NET 动态接口中的内部公共方法必须明确标记 `[NonAction]`；不能用 `App.HttpContext != null` 判断“是否来自管理端”，因为登录、授权和普通 CRUD 在 HTTP 请求内调用缓存等内部服务时同样存在 `HttpContext`。此类误判会让正常账号登录或业务写入被管理员校验错误拦截。
- 后端权限回归至少覆盖 `superadmin`、`admin`（`SysAdmin`）和普通账号三类身份：超级管理员验证全局维护能力，系统管理员验证菜单按钮授权，普通账号验证无权限接口被 403 拒绝；不能只用超级管理员账号验收页面。
- 涉及权限、菜单、租户、角色、用户、组织的数据写入，必须同时检查前端提交数据是否去重、后端是否有兜底去重、数据库是否有唯一约束或业务保护，避免把权限表、关系表写脏。
- 高风险按钮不能当成普通刷新按钮处理；必须写清楚它会改什么数据、改哪个上下文、成功后用户身份和菜单权限会发生什么变化。
- 租户管理已经发现并修复过 `SysTenantMenu` 重复授权问题：清理了 73 组重复 `(TenantId, MenuId)`，并增加唯一索引 `UX_SysTenantMenu_TenantId_MenuId`。后续同类页面要以此为反例，先验证数据副作用再继续迁移。

## 进度追加：注册方案

- `system/userRegWay` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面。
- 注册方案页已接入真实接口：列表、新增、编辑、删除、租户筛选、角色/机构/职位绑定。
- 删除注册方案属于高风险动作：后端会关闭引用该方案的租户注册功能并清空默认注册方案，页面已在确认弹窗中明确提示。

## 进度追加：通知公告

- `system/notice` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面。
- 已接入真实分页、通知类型/状态字典、新增、编辑、发布、删除和按钮级权限接口。
- 发布属于不可逆高风险动作：后端会删除并重建该公告的全部账号接收记录，同时广播在线用户；当前后端没有撤回接口，发布后禁止编辑和删除。
- 页面列表只展示纯文本摘要，编辑使用 Vben 内置 Tiptap 富文本组件，避免继续引入旧 Element Plus 编辑器依赖。
- 已完成真实发布验收：测试公告发布后，6 个有效账号生成 6 条接收记录，重复接收人 0 组，失效用户引用 0 条。
- 发布链路增加三层数据保护：前端发布中锁定按钮、后端事务与草稿状态原子校验、数据库 `(NoticeId, UserId)` 唯一索引；以后所有关系表写入继续按“前端防重复、后端幂等、数据库约束”验收。

## 当前下一步（2026-07-13）

- 通知公告真实 CRUD 与发布链路已验收，后端防并发保护与接收关系唯一约束已完成。
- `system/weChatUser` 第三方账号已完成迁移：真实查询、新增、编辑、删除、平台字典、绑定状态和修改记录均已接入；浏览器真实新增、编辑、删除验收通过，测试数据已清理。
- 第三方账号接口已改用安全 DTO，管理端响应和保存参数不再包含 `SessionKey`、`AccessToken`、`RefreshToken`；同平台 `OpenId` 增加后端重复校验。
- 个人中心已进入完成验收状态，详细结果见下方“进度追加：个人中心”。

## 进度追加：个人中心

- `system/user/component/userCenter` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面。
- 已接入真实个人资料读取和保存、组织树定位、SM2 修改密码、头像上传、PNG 签名上传及手写电子签名；签名绘制使用 `signature_pad`，不自行实现轨迹算法。
- 个人资料接口已从完整 `SysUser` 实体改为专用安全 DTO，保存时只允许修改当前账号的姓名、昵称、手机、邮箱、生日、性别、地址、简介和备注，不能再借个人中心修改租户、账号类型、状态、机构等管理字段。
- 浏览器已完成资料读取、原值保存、组织信息、修改密码表单和手写签名弹窗验收；头像和签名上传会覆盖当前超级管理员的真实文件，本轮未擅自上传测试图片。
- 下一步迁移 `system/ldap` AD 域配置页面，先核对同步域账号、同步组织等高风险写操作的数据范围和幂等性，再实现页面。

## 进度追加：AD 域配置

- `system/ldap` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，已接入分页查询、新增、编辑、删除、同步域用户、同步域组织和按钮权限。
- 管理端接口不再返回加密后的 `BindPass`；列表和详情只返回 `HasBindPass`，编辑时绑定密码留空代表保持原密码不变。
- LDAP 配置详情、编辑、删除和同步统一执行租户访问校验，非超级管理员不能跨租户读取或修改配置。
- 域用户同步在租户内按账号去重，并在事务内替换域账号镜像；不会删除本地 `SysUser`。数据库增加 `(TenantId, Account)` 唯一索引作为最后一道保护。
- 域组织同步已移除旧版“先清空整张机构表再插入”的危险实现，改为按机构编码幂等新增或更新，保留已有机构 Id，不自动删除本地机构，避免用户、职位和数据范围引用失效。
- 同步域用户和同步域组织均增加影响范围说明、二次确认、执行中状态和新增/更新/总数反馈。
- 已通过 ESLint、`vue-tsc`、后端核心项目编译以及浏览器页面/弹窗/控制台验收。当前数据库没有可用 LDAP 配置，本轮未连接外部目录服务器执行真实同步。
- 后端启动日志仍存在枚举转字典任务与配置读取并发使用 MySQL 连接的既有告警，服务可正常返回 200；该项列入后端稳定性专项，不与 LDAP 页面验收混为一项。

## 当前下一步（2026-07-13，续）

1. 迁移 `system/config` 参数配置，先审计配置值的租户覆盖规则、敏感配置脱敏和缓存刷新，再实现真实 CRUD。
2. 修复后端启动阶段 `This MySqlConnection is already in use` 并发告警，确认枚举转字典任务和错误邮件配置读取使用独立连接。
3. 参数配置完成后继续文件管理、日志管理和任务调度；重型审批流放在普通运维页稳定后处理。

## 进度追加：参数配置与文件管理

- `platform/config` 参数配置已完成 Vben 迁移，真实新增、编辑租户覆盖值、删除均已验收，测试数据已清理。
- 参数接口不再返回默认密码等敏感配置明文；普通租户只能修改自己的覆盖值，只有超级管理员可维护全局参数定义。
- `platform/file` 文件管理已完成 Vben 迁移，支持租户筛选、名称/后缀/时间查询、上传、图片/PDF 预览、下载、业务信息编辑、删除和修改记录。
- 文件上传已补齐“是否公开”；文件编辑改为专用 DTO，只允许修改名称、类别、公开状态和关联信息，禁止回写租户、存储路径、桶、MD5 等存储元数据。
- 公开文件查询显式排除软删除记录；公开文件读取仍需命中文件表且未删除，不能通过任意 URL 读取服务器文件。
- 文件管理已完成真实上传、下载、编辑、删除验收；测试记录和磁盘实体文件均已清理。

## 当前下一步（2026-07-13，再续）

1. 迁移 `system/cache` 缓存管理，明确单键删除、按前缀删除、清空全部缓存的影响范围并保留二次确认。
2. 迁移四类日志页面，统一查询栏、详情弹窗、长 JSON/异常堆栈展示与敏感字段脱敏。
3. 修复启动阶段 MySQL 连接并发告警，再进入定时任务和打印等较重模块。

## 进度追加：缓存与日志管理

- `platform/cache` 缓存管理已完成 Vben 迁移：真实缓存分组树、搜索、展开/折叠、值查看、单键删除、分组删除和应用缓存清理均已接入。
- 缓存管理接口仅允许超级管理员访问；敏感键值自动脱敏；“清空全部”只清理 Admin.NET 应用前缀，禁止误清共享 Redis 中其他系统的数据。
- `log/oplog` 操作日志已完成 Vben 迁移：租户/日期/模块/动作/账号/IP/耗时/状态筛选、详情、导出和按租户清理均已接入真实接口。
- 操作日志详情会对密码、令牌、Cookie、连接串、私钥等字段递归脱敏；失败状态改为数据库级筛选，分页总数准确。
- `log/vislog` 访问日志已完成 Vben 迁移：真实分页筛选、客户端信息、访问地点、耗时高亮、详情弹窗和按租户清理均已接入。
- 访问日志原有整表 `TRUNCATE` 清空接口已改为超级管理员限定、按租户可控的删除；旧 Web 不传请求体的调用仍保持兼容。地图列表经纬度有效性判断错误已修复。
- 缓存清空、操作日志清空、访问日志清空均只验收了二次确认和范围提示，没有执行真实清空，避免破坏现有运行数据。
- ESLint、`vue-tsc`、后端 net8.0/net6.0 双目标编译通过；浏览器已验证操作日志与访问日志真实数据、详情弹窗和高风险操作取消链路。

## 当前下一步（2026-07-13，日志阶段）

1. 继续迁移异常日志 `system/log/exlog/index`，复用操作日志详情布局和统一脱敏器。
2. 迁移差异日志 `system/log/difflog/index`，重点核对实体变更前后 JSON 的可读对比和敏感字段脱敏。
3. 日志四页完成后修复启动阶段 MySQL 连接并发告警，再进入定时任务模块。

## 进度追加：四类日志与启动稳定性

- 异常日志 `log/exlog` 已完成真实分页、租户/日期/模块/账号/IP/耗时筛选、详情、导出和按租户清理；详情请求参数、返回结果、消息和异常堆栈统一脱敏。
- 差异日志 `log/difflog` 已完成真实分页、租户/日期筛选、字段变更前后对照、SQL、参数和业务数据详情，以及按租户清理。
- 差异数据不再使用 `v-html` 渲染数据库内容，避免日志内容注入；参数名或字段名命中密码、令牌、连接串等敏感规则时，会联动遮蔽对应值、变更前值和变更后值。
- 四类日志清理接口均禁止普通管理员调用，超级管理员可按当前选中租户清理；页面保留明确范围提示和二次确认。本轮只验证确认与取消，没有执行真实删除。
- 修复数据库日志写入器长期复用依赖注入作用域和 MySQL 连接的问题：每次写入创建独立作用域、独立 SqlSugar 连接；参数配置查询也使用独立仓储连接。
- 后端重启后连续检查启动日志，`This MySqlConnection is already in use` 与 `Can't replace active reader` 均为 0；5005、8888、5666 三端口均返回 200。

## 当前下一步（2026-07-13，运维模块）

1. 迁移定时任务 `system/job/index` 与任务仪表盘，先审计新增、暂停、启动、立即执行、删除任务的幂等性和并发保护。
2. 迁移系统监控、在线用户等只读或低风险页面，再处理强制下线等写操作。
3. 审计并升级存在安全公告的后端依赖，优先处理 `System.Linq.Dynamic.Core` 高危版本，其次是 `log4net`、`MailKit`。

## 进度追加：任务调度（2026-07-14）

- `system/job/index` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，接入作业分页、分组筛选、触发器展开、执行记录和调度集群查询。
- 已接入作业与触发器的新增、编辑、删除，以及启动、暂停、取消、立即执行、全局启动/暂停、唤醒和持久化等真实接口；所有会改变调度状态的操作均有影响说明和二次确认。
- 后端为任务调度全部写操作增加超级管理员校验，并补齐作业、触发器不存在时的业务校验，不能再仅依赖前端隐藏按钮保护全局任务。
- 修复任务表格行标识错误：现在按作业编号生成唯一行标识，展开一个作业只显示该作业的触发器，不再同时展开全部作业。
- 页面读取到 5 个真实作业和 5 个真实触发器，运行记录接口返回真实数据；集群接口当前返回空列表，页面按空状态正常展示。
- 已通过 ESLint、`vue-tsc`、后端 net8.0 编译和浏览器验收。任务调度具有真实业务副作用，本轮只验收查询、展开、表单、记录、集群和确认弹窗，不执行真实任务，不暂停或删除现有任务。

## 追加规则：调度与后台执行类功能

- 任务、同步、批处理、消息发布、缓存清理等后台执行类功能，页面验收不得把“真实执行成功”当成默认检查手段；先验证读取、参数校验、权限、影响范围说明、二次确认和取消链路。
- 必须执行真实动作时，要先确认测试对象、影响的数据范围、是否可回滚和是否会调用外部系统；执行后同时核对运行记录和业务数据，不能只看成功提示。
- 全局调度配置和脚本任务只能由超级管理员修改；前端按钮权限、后端身份校验、目标存在性校验三层必须同时成立。

## 当前下一步（2026-07-14，任务阶段）

1. 迁移任务仪表盘 `system/job/dashboard`，重点展示运行趋势、成功率、错误任务和近期执行记录，不重复堆砌大卡片。
2. 迁移系统监控与在线用户页面，先完成只读指标，再为强制下线增加目标用户、当前租户和二次确认保护。
3. 建立后端依赖升级清单并逐项回归，优先处理 `System.Linq.Dynamic.Core` 高危版本，其次处理 `log4net` 和 `MailKit`。

## 进度追加：任务看板（2026-07-14）

- 已核对旧版实现：任务看板不是左侧菜单，而是任务调度页中的隐藏入口；旧 Vue 页面为空壳，实际通过 iframe 打开后端 `/schedule` 看板。
- 新版保持旧版信息架构，不新增左侧菜单。在任务调度页保留“任务看板”按钮，并按当前用户是否拥有任务调度菜单权限动态注入隐藏路由 `/platform/job/dashboard`。
- 看板改为 Vben 原生只读页面，使用真实作业、触发器、运行记录和集群接口，展示作业数量、活动/暂停触发器、最近记录健康率、异常触发器、调度节点、历史记录、7 天趋势、触发器状态和近期执行记录。
- 看板统计最近 500 条运行记录，数据库累计记录单独显示；当前真实数据为 5 个作业、5 个触发器、2931 条历史记录和 1 个需要关注的钉钉同步触发器。
- 统一抽取 Furion 触发器状态定义。状态 5 正确显示为“失败后就绪”，状态 8 显示为“已达次数上限”，并补齐无触发时间、未启动、未知触发器和未知处理程序等状态，避免两个页面翻译不一致。
- 已通过 ESLint、`vue-tsc` 和浏览器验收；确认任务调度页入口可访问、趋势图正常绘制、左侧菜单未出现新增项。

## 追加规则：旧版信息架构对齐

- `E:\admin.NET\Web` 永久作为旧版对照基线，只允许读取、运行和浏览器验收，禁止修改源码、配置、依赖、锁文件、格式或生成文件。
- 任何 UI 翻新、兼容适配和页面修复只能写入 `E:\admin.NET\vben-web`；确需修改后端时，只能写入 `E:\admin.NET\Admin.NET`，并在计划和验收记录中说明数据影响与兼容性。
- 执行格式化、自动修复、依赖安装和批量替换时必须显式排除旧版 `Web` 目录，不能让工具产生隐式变更。
- 每个模块验收都保留旧版 `8888` 页面作为功能范围和交互参考，不以新版实现覆盖旧版文件。
- 迁移新模块前必须先核对旧版菜单、隐藏路由、页内按钮、iframe 和后端内置页面；不能因为发现一个组件文件就擅自新增左侧菜单。
- 旧版页内入口继续作为页内入口，隐藏功能继续使用隐藏路由；只有用户明确要求调整导航结构时才新增或移动菜单。
- 旧版 iframe 页面可以重做为 Vben 原生页面，但必须保留原入口、权限边界和功能范围，并说明替换了哪个后端内置页面。

## 当前下一步（2026-07-14，监控阶段）

1. 迁移系统监控页面，先核对旧版是否使用后端 iframe、接口轮询频率以及服务器敏感信息展示范围。
2. 迁移在线用户页面，真实接入租户筛选、连接信息和强制下线；强制下线必须校验目标租户、禁止误下线当前账号并保留二次确认。
3. 任务调度与任务看板稳定后，再建立后端依赖升级清单并逐项回归。

## 进度追加：系统监控与在线用户（2026-07-15）

- 系统监控已按旧版功能范围完成 Vben 原生迁移，沿用页内入口和原有权限，不新增左侧菜单。
- 在线用户保持旧版顶部工具栏入口，接入 Admin.NET 原有 SignalR 在线连接表和实时刷新机制；列表中的一条数据代表一个浏览器或标签页连接，不代表一个唯一账号。
- 同一账号在多个浏览器、无痕窗口或标签页登录会显示多条会话，这是正常状态。新版明确标识“当前窗口”“同账号其他窗口”“其他账号”，并将总数命名为“在线会话”，避免被误认为重复账号。
- 当前窗口只显示“正在使用”，不允许误下线；同账号其他窗口可单独退出，其他账号按权限执行强制下线。确认窗口会说明具体影响，不使用含糊的“确认操作”文案。
- 强制下线在服务端重新查询真实连接，不信任前端传入的用户、租户或账号类型；同时校验操作者身份、租户边界、当前连接和目标账号级别。系统管理员不能下线其他管理员，超级管理员可跨租户管理。
- 在线用户读取和下线接口仅允许超级管理员或系统管理员访问；前端按钮权限只负责交互展示，不能替代服务端校验。
- 本轮浏览器验收只读取真实在线会话，不自动执行强制下线，避免中断用户当前工作。强制下线的破坏性验收应使用单独测试账号和独立浏览器窗口。

## 追加规则：会话与实时连接

- 在线用户、即时消息、通知和 SignalR 功能必须区分“账号”“登录令牌”“浏览器窗口”“实时连接”四个概念，不能为了列表好看而按账号强行去重。
- 当前连接标识必须由标准 SignalR 协商流程取得；不能只凭账号、IP、浏览器名称或列表顺序猜测当前窗口。
- 下线操作必须按连接精确执行，并在后台再次核对连接是否仍存在、是否属于当前租户以及是否允许管理目标账号。
- 自动化验收默认不得点击强制下线、断开全部连接等破坏性按钮；需要真实验证时，先创建可识别的测试会话，执行后同时核对目标窗口、数据库在线记录和当前管理窗口。

## 当前下一步（2026-07-15）

1. 完成在线用户当前窗口识别、同账号其他会话退出和三类账号权限回归验收。
2. 按旧版信息架构核对并迁移下一个现有模块，不新增旧版不存在的菜单。
3. 建立后端依赖升级清单，优先评估 `System.Linq.Dynamic.Core`，再处理 `log4net` 和 `MailKit`，每次升级单独回归后端启动、登录、动态菜单和任务调度。

## 进度追加：行政区域（2026-07-15）

- `platform/region` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保持旧版左侧区域树、右侧列表、查询、新增、编辑、删除和官方行政区划同步功能范围。
- 区域树采用按需加载，默认不展开整棵全国区域树；支持已加载节点搜索、全部折叠、展开已加载节点、回到根节点和刷新。
- 新增与编辑使用紧凑弹窗，上级区域改为树选择，不再要求用户手工填写难以理解的父级 Id；行政代码前后端统一校验 6、9 或 12 位。
- 后端按行政代码执行唯一校验，并为 `SysRegion.Code` 增加数据库唯一索引；新增区域的层级由父节点计算，移动节点时同步修正全部下级层级，禁止把父节点移动到自己的子树中。
- 删除仍兼容旧版的级联删除行为，但新版明确提示会删除所选区域及全部下级，后端返回实际删除数量；自动化验收不执行真实删除。
- 全国行政区划同步仅允许超级管理员执行，页面要求输入“同步”二次确认。数据源已改为中华人民共和国民政部公开的 2024 年县以上行政区划代码，不再依赖旧统计局地图页面。
- 同步执行顺序固定为“获取官方数据 -> 校验编码唯一性和父子完整性 -> 开启事务 -> 替换数据 -> 提交”。抓取、解析、校验或写入任一步失败都会回滚，原数据保持不变。
- MySQL 写入使用事务内每批 500 条的普通批量插入，不启用 `local_infile`，避免为了同步功能扩大数据库文件导入权限。
- 已在用户明确确认后执行真实同步，成功写入 `3213` 条：省级 `34`、市级 `333`、区县级 `2846`。分页接口返回总数 `3213`，未发现重复编码或孤立父节点。
- 浏览器已验收“湖北省 -> 武汉市 -> 江岸区”三级懒加载和列表联动；江岸区代码为 `420102`，页面显示层级为“区县级”。
- 行政区划分页已增加请求顺序保护，快速连续切页时只接收最后一次请求结果；表格改为内部纵向滚动，切页后自动回到首行，并增加页码快速跳转。浏览器已验收连续切换第 2、3 页、跳转第 65 页以及区域树筛选后重置第 1 页。
- 新版分页能力已抽取为公共配置，并接入账号、角色、租户、字典、参数、通知、文件、LDAP、任务调度、打印模板、行政区域、微信用户、系统监控和四类日志等已迁移页面；不再由各页面单独维护分页选项。
- 已通过 ESLint、`vue-tsc`、后端 net8.0 完整编译和真实接口/浏览器验收；5005、8888、5666 均正常监听，旧版 `Web` 目录无任何改动。

## 当前下一步（2026-07-15，行政区域之后）

1. 对打印模板执行一次人工业务验收：创建临时模板、拖入文本组件、保存、重新编辑、预览、浏览器打印，确认后再删除临时模板。该流程涉及真实写入和浏览器打印权限，不由自动验收擅自执行。
2. 继续遵守“旧版只读参考”规则：先盘点旧版页面、页内入口、接口和权限，再在 `vben-web` 中重建，不新增旧版不存在的菜单。
3. 盘点并迁移旧版下一个现有模块，优先选择仍处于迁移占位状态且业务边界清晰的页面。

## 进度追加：打印模板（2026-07-15）

- `platform/print` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保留旧版租户筛选、模板名称查询、分页、新增、编辑、删除、状态、排序和修改记录能力。
- 新版继续使用 Admin.NET 原有 `vue-plugin-hiprint 0.0.58-fix` 模板格式，旧版保存的 hiprint JSON 可直接载入编辑，不另造一套不兼容的模板协议。
- 全屏设计器已提供 13 种基础拖拽组件：文本、图片、长文本、表格、空白表格、HTML、自定义文本、横线、竖线、矩形、椭圆、条形码和二维码。
- 设计器已补齐纸张预设、自定义宽高、缩放、旋转、组件属性、模板设置、测试数据 JSON、预览、浏览器打印、PDF 导出和清空画布二次确认。
- 模板新增、更新、删除均接入 Admin.NET 原生 `sysPrint` 真实接口并继续使用 `sysPrint:page/add/update/delete` 按钮权限；删除前明确提示业务打印入口可能受影响。
- 当前默认租户没有打印模板。本轮自动验收只执行查询和设计器只读操作，未创建、修改、打印或删除真实模板。
- 已通过 ESLint、`vue-tsc`、生产构建和浏览器验收。设计器成功渲染 1 个纸张面板、1 个组件分组和 13 个拖拽组件，5005、8888、5666 均正常监听，旧版 `Web` 目录无改动。

## 进度追加：模板管理（2026-07-17）

- `platform/template` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保持旧版名称、编码、类型、分组筛选，以及新增、编辑、删除、预览和修改记录功能范围。
- 页面接入真实 `sysTemplate` 分页、分组、字典、渲染、添加、更新和删除接口；权限码按后端菜单定义使用 `sysTemplate:*`，修正旧版页面误用 `sysConfig:*` 的问题，未修改 Admin.NET 后端源码。
- 编辑弹窗提供基础信息和模板内容两个标签页，支持富文本与纯文本、`@(参数名)` 自动识别、预览参数维护及真实后端渲染。富文本预览使用无脚本权限的沙箱 iframe，不直接用 `v-html` 执行返回内容。
- 浏览器已验收空数据状态、新增弹窗、类型字典、参数自动识别和真实渲染结果“你好，示例1”；验收未保存、更新或删除任何数据库记录。
- 下一模块继续按旧版菜单顺序盘点“动态插件”和“开放接口”，先核对后端安全边界后再选择业务边界清晰的一项迁移。
- hiprint 生产懒加载块约 `3 MB`，仅进入打印模板模块时加载；该第三方旧版依赖内部包含 `eval`，构建工具已给出安全警告。后续升级或替换打印引擎时必须先验证历史模板 JSON 兼容性，不能直接无损替换假设。

## 进度追加：动态插件（2026-07-17）

- `platform/plugin` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保持旧版租户筛选、功能名称查询、新增、编辑、删除、状态、排序和修改记录功能范围，未新增旧版不存在的菜单。
- 页面真实接入 Admin.NET 原生 `sysPlugin/page/add/update/delete` 接口，并继续使用 `sysPlugin:*` 权限；未修改动态插件后端服务和 Admin.NET 启动、鉴权流程。
- 动态插件的真实机制是提交 C# 源码，由服务器即时编译程序集并注册 WebAPI，不是普通文件上传。新增、更新、删除均增加运行影响说明和二次确认，自动化验收禁止执行这些写操作。
- 新版编辑器会回填数据库已有的 `CsharpCode`，修正旧版编辑弹窗创建编辑器后没有设置已有源码、容易把插件更新为空代码的问题；源码少于 100 个字符时前端拒绝提交。
- “状态”字段按当前后端实际能力标注为记录状态：现有 `SysPluginService` 不会因为状态切换为禁用就自动卸载已编译程序集，页面不把它误述为运行开关。
- 浏览器已验收真实租户列表、空数据状态、新增弹窗和源码编辑区；当前数据库动态插件为 0 条，未创建测试插件，避免向运行中的 API 注册无业务用途的程序集。
- 已通过 ESLint、`vue-tsc` 和动态菜单适配 5 项单元测试；旧版 `Web` 目录未修改。

## 追加规则：动态代码与运行时扩展

- 动态 C#、脚本、SQL、表达式和运行时插件属于远程代码执行能力，只能由受信任管理员维护；前端权限、后端身份校验和操作审计缺一不可。
- 自动化验收默认只验证读取、表单校验、风险说明、二次确认和取消链路，不得为了证明按钮可用而向生产式运行环境写入测试代码。
- 更新动态程序集前必须考虑编译失败后的回退能力。当前后端采用“先卸载旧程序集、再编译新程序集”，存在更新失败后当前接口暂时不可用的风险；后续如调整后端，应改为先编译验证新程序集，再原子替换旧程序集，并增加版本和回滚记录。
- 状态字段只有在服务端真正参与加载、卸载和启动恢复逻辑时才能称为“启停开关”；不能仅凭数据库状态标签向用户承诺接口已经停用。

## 当前下一步（2026-07-17，动态插件之后）

1. 按旧版菜单顺序迁移 `system/openAccess/index` 开放接口，先审计访问令牌、签名、IP 白名单、调用日志和密钥展示边界。
2. 若开放接口只是后端 iframe 或页内入口，保持旧版信息架构，不擅自新增菜单或扩大功能范围。
3. 动态插件未来需要真实业务验收时，使用单独测试插件和可回滚源码，验证新增、调用、编辑替换、删除卸载全过程，并同时检查运行日志和接口路由。

## 进度追加：开放接口（2026-07-17）

- `platform/openAccess` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保持旧版身份标识查询、新增、编辑、删除、生成签名和使用说明功能范围，增加密钥轮换能力但未新增菜单。
- 页面接入真实 `sysOpenAccess` 接口和 `sysOpenAccess:*` 权限；列表使用新增的安全分页接口，永不向浏览器返回 `AccessSecret`，密钥列只显示“已保护”。
- 新增身份时密钥可以由后端安全随机生成，保存前可复制；编辑身份默认保留现有密钥，不再依赖列表明文回填。轮换密钥会让旧密钥立即失效，新密钥只展示一次。
- 生成签名改为提交身份 Id、方法、路径、时间戳和随机数，由后端读取已保存密钥计算 HMAC-SHA256；密钥不参与浏览器请求和响应。
- 后端沿用现有 `UserManager` 身份体系，没有修改 `Startup.cs` 或重复添加认证中间件。开放身份管理仅允许超级管理员或系统管理员；系统管理员只能管理当前租户记录。
- 新增和更新时，后端强制校验绑定租户存在、绑定用户存在且确实属于该租户，防止构造租户与用户不一致的身份声明。
- 修复更新身份标识后的缓存处理：同时清理旧 accessKey 和新 accessKey 缓存，避免旧身份在缓存期内继续生效；删除和密钥轮换也会立即清理认证缓存。
- 为不破坏旧版参考页，原 `page/add/update/delete/secret/generateSignature` 接口仍保留；新版使用 `pageSafe/safe/rotateSecret/generateStoredSignature` 安全接口。
- “新手使用指南”已改为四部分：概念说明、逐步配置、完整签名请求、按钮含义与常见错误。新增弹窗也直接解释身份标识、绑定租户和绑定用户的作用。
- 当前数据库开放接口身份为 0 条。浏览器已验收安全分页、租户读取、密钥随机生成、新增弹窗和指南四个标签页；未保存身份、轮换密钥或删除数据。
- 已通过前端 ESLint、`vue-tsc`、动态路由 6 项单元测试和后端 net8.0 完整入口编译；Swagger 已确认全部安全接口注册成功。

## 追加规则：开放接口与机器身份

- 开放接口身份属于机器凭证，不是普通用户登录方式。必须优先创建专用、最小权限账号，禁止默认绑定超级管理员或日常管理账号。
- `accessSecret` 不得出现在分页、详情、日志、URL、前端持久化状态或聊天记录中；只允许创建或轮换时一次性展示，并由调用方保存到服务端环境变量或密钥管理系统。
- 生成签名必须优先使用服务端已保存密钥，不能让浏览器为了调试重新获取密钥。每次请求必须使用当前秒级时间戳和全新 nonce，服务端继续执行时间偏差与重放校验。
- 绑定租户和绑定用户必须在服务端交叉验证，不能信任前端下拉框；系统管理员只能管理当前租户，跨租户机器身份只允许超级管理员处理。
- 密钥轮换、删除身份和变更绑定用户都会影响外部系统，必须显示影响范围和二次确认；自动化验收默认不得执行最终写操作。

## 当前下一步（2026-07-17，开放接口之后）

1. 按旧版菜单继续盘点 `system/infoSetting/index` 系统配置，先区分站点外观配置、登录页配置和后端运行参数，避免与已迁移的参数配置重复。
2. 核对系统配置是否包含 Logo、备案、版权、登录背景等文件上传；保持旧版真实接口和租户覆盖规则，不把敏感运行配置放到页面。
3. 对开放接口做真实业务联调时，使用专用测试账号和明确测试接口，完整验证签名成功、过期时间戳、重复 nonce、错误密钥和越权接口五种情况，完成后删除测试身份。

## 进度追加：系统配置（2026-07-17）

- `platform/infoSetting` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保持旧版 Logo、主副标题、系统描述、水印、版权、ICP备案和用户注册方案功能范围，未增加旧版不存在的配置项。
- 页面读取并保存 Admin.NET 原生 `sysConfig/sysInfo`、`sysConfig/saveSysInfo` 接口；系统信息继续按当前租户落在租户表，不与运行参数配置混用。
- 新版登录页、侧栏 Logo、系统名称、浏览器标题和页面水印已接入当前租户系统信息。保存后当前页面立即刷新品牌，其余已打开页面刷新后生效，不需要重新部署前端。
- 表单按品牌信息、版权备案、用户注册三组组织，使用紧凑双栏和实时预览；在窄屏下自动单栏，无横向溢出。Logo 仅接受 PNG/JPG/JPEG，限制 2MB。
- 后端保存接口增加超级管理员/系统管理员校验，不能只依赖菜单可见性；同时补齐字段长度、ICP备案链接协议和 Logo Data URI/大小/扩展名校验，长度与 `SysTenant` 数据库实体保持一致。
- 用户注册开启时必须选择当前租户真实存在的注册方案；没有方案时页面明确指引先前往“注册方案”创建，不显示难以理解的空白选择器。
- 浏览器已读取默认租户真实配置并确认品牌联动，自动化验收未点击保存、未上传 Logo、未修改租户数据。
- 已通过前端 ESLint、`vue-tsc`、动态路由 7 项单元测试、后端 net8.0 完整入口编译；5005、5666、8888 均返回 200，旧版 `Web` 目录未修改。

## 追加规则：租户品牌与公开站点信息

- 公开登录页只能读取 Logo、标题、描述、水印、版权、备案和注册入口等站点信息，不能混入数据库连接、密钥、运行参数或内部租户配置。
- 保存接口必须以后端身份校验为准；允许匿名读取站点信息不等于允许匿名或普通用户修改。
- 上传的品牌文件必须同时校验 MIME、扩展名、Base64 内容和大小，不能只依赖前端 `accept`；SVG 等可执行标记格式默认不进入公开品牌上传链路。
- DTO 长度、实体字段长度、前端 `maxlength` 和表单提示必须一致，避免出现页面可填但数据库无法保存的假成功路径。
- 品牌配置保存后的验收同时检查数据库返回、登录页、后台侧栏、页面标题和水印，不以单个“保存成功”提示作为完成依据。

## 当前下一步（2026-07-17，系统配置之后）

1. 按旧版菜单顺序盘点 `system/weChatPay/index` 微信支付，先确认它是配置、订单查询还是实际支付操作，并核对证书、APIv3 密钥、商户号等敏感信息的存储和返回边界。
2. 支付、退款、关单、转账等资金类操作默认只做只读审计和取消链路，不在自动化验收中执行真实交易；任何写操作必须明确金额、订单、商户和可回滚性。
3. 若微信支付依赖外部平台配置或证书，页面需提供非技术人员可理解的字段说明、配置步骤和错误排查，但不得展示完整密钥或把证书内容写入日志。

## 进度追加：微信支付（2026-07-17）

- `platform/wechatpay` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保持旧版订单查询、创建 Native 扫码支付测试单、付款二维码、退款记录和全额退款功能范围，未新增菜单。
- 旧版“新增模拟数据”实际会调用微信支付真实下单接口，新版改名为“创建扫码支付测试单”，并明确说明只有扫码确认后才扣款，避免把真实资金链路误认为本地模拟数据。
- 页面新增不泄密的支付环境检查，仅返回 AppId、商户号、APIv3 密钥、证书序列号、证书文件、支付回调和退款回调是否配置，不返回任何密钥值、证书路径或私钥内容。
- 当前环境 7 项配置均未就绪，页面只允许查询本地订单；创建订单、同步微信状态和退款按钮均禁用，后端也会再次拒绝，不能通过修改前端绕过。
- 后端继续使用 `Admin.NET.Web.Core/Startup.cs` 的全局 JWT 和 `JwtHandler` 按钮权限，没有重复注册认证中间件。支付表没有租户字段且商户配置全局共享，因此支付管理台额外限制为超级管理员或系统管理员。
- 修复支付分页缺少按钮权限映射导致任意已登录用户可查询全局订单的问题：分页接口现在执行后端管理员身份校验。
- 创建 Native 订单在后端校验配置完整性、金额大于 0、商品描述和附加字段长度；调用失败不再把整个微信响应序列化到日志。
- 退款不再信任前端传入的原订单总金额：服务端按商户订单号重新读取真实订单，只允许 `SUCCESS` 订单，核对当前商户、累计已申请退款和剩余可退金额，并阻止单进程内同一订单并发重复退款。
- 全额退款弹窗明确显示订单号、商品、实际金额，要求填写原因并输入“退款”；自动化验收不执行创建订单、微信状态同步或退款。
- 使用说明补齐配置位置、证书边界、HTTPS 回调要求和按钮含义；私钥与 APIv3 密钥继续只存在后端配置和证书文件中。
- 当前数据库支付订单为 0 条。已通过前端 ESLint、`vue-tsc`、动态路由 8 项单元测试、后端 net8.0 完整入口编译和浏览器只读验收；5005、5666、8888 均返回 200，旧版 `Web` 目录未修改。

## 追加规则：支付与资金操作

- 支付页面中的“测试单”“模拟单”只要调用第三方支付接口，就必须按真实资金链路展示和确认，不能用弱化文案掩盖业务副作用。
- 退款金额、原订单金额、商户号、支付状态和剩余额度必须由后端按订单重新核对，禁止信任前端隐藏字段或列表行数据。
- 支付查询、退款和商户配置必须先明确租户模型。当前支付实体不含租户字段，只能作为全局商户管理台由超级管理员/系统管理员操作；未来要开放租户财务角色，必须先完成支付实体与商户配置的租户隔离迁移。
- 支付密钥、APIv3 密钥、商户私钥、证书内容和完整签名材料不得返回浏览器、写入普通日志、截图或聊天记录；前端只能查看“已配置/未配置”。
- 自动化验收默认只执行本地订单查询、配置状态、表单校验、风险提示和取消链路，不创建真实支付单、不扫码、不查询第三方状态、不退款。
- 退款并发保护在当前单实例进程内生效；生产部署多个后端实例前，必须升级为数据库锁或分布式锁，并用唯一幂等键约束同一退款请求。

## 当前下一步（2026-07-17，微信支付之后）

1. 按旧版菜单顺序盘点 `system/update/index` 系统更新，确认它是版本提示、下载更新包还是会覆盖服务器文件；涉及替换程序集时默认只读审计。
2. 核对系统更新是否在旧版菜单中可见、是否有独立按钮权限、是否依赖外部版本服务；保持旧版入口与功能范围，不把更新能力扩展成任意文件上传或远程执行。
3. 系统更新后继续盘点 `system/stressTest/index` 等剩余占位模块，压力测试必须限制目标地址、并发量、持续时间和管理员身份，自动化验收不得对真实服务发起压力流量。

## 进度追加：系统更新（2026-07-17）

- `platform/update` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保留旧版备份列表、更新日志、执行更新、还原备份、清空日志和 WebHook 密钥功能范围，没有新增菜单。
- 页面新增不泄密的配置就绪检查，只展示仓库、分支、目标框架、运行平台及令牌/部署目录/发布参数是否就绪，不返回仓库令牌和部署目录绝对路径。
- 当前开发环境的仓库令牌仍是占位值，部署目录 `D:\Admin.NET` 不存在，因此执行更新、还原和获取 WebHook 密钥均被禁用；只读备份与日志接口可正常使用。
- 更新和还原属于服务器程序文件覆盖操作，后端统一限制为超级管理员或系统管理员，并增加单进程互斥锁，防止同一实例内并发更新、更新与还原互相覆盖。
- 远端仓库名、部署目录、临时目录和备份文件名增加路径边界校验；更新包与备份包解压前检查条目路径、文件数量和解压总体积，防止目录穿越及异常压缩包。
- 更新备份文件名改为秒级时间戳，避免同一天多次更新覆盖当天唯一备份；还原只能选择服务端备份列表中的文件名，前端不能提交任意文件路径。
- WebHook 保留旧版匿名入口，但增加五分钟时间戳有效期、Base64 格式校验和恒定时间签名比较，不再把请求签名写入更新日志；匿名 WebHook 不会绕过配置检查和更新互斥锁。
- 发布命令同时消费标准输出和错误输出并检查退出码，避免错误流填满导致进程阻塞，也不会把编译失败误报为更新成功。
- 新版使用说明明确：更新会拉取、编译、备份并覆盖后端程序，不是只检查版本；还原不恢复数据库，也不会删除备份之后新增的文件；清空只影响本页面更新日志。
- 已通过前端 ESLint、`vue-tsc`、动态路由 9 项单元测试、后端核心与完整入口 net8.0 编译及浏览器只读验收。自动化验收未执行更新、还原、清空日志或读取真实 WebHook 密钥，旧版 `Web` 未修改。

## 追加规则：系统发布与回滚

- 任何“更新、发布、部署、还原”按钮都按服务器文件写操作处理，必须同时具备后端管理员校验、独立按钮权限、互斥保护、路径边界、影响说明和二次确认，不能依赖前端按钮隐藏。
- 自动化验收默认只检查配置状态、备份列表、日志、表单校验和取消链路，不得在当前开发机或生产式环境中执行真实发布、覆盖或还原。
- 程序目录备份不等于数据库备份。更新前必须另行确认数据库备份、维护窗口、服务器 SDK/权限和重启方案；还原页面不得向用户暗示它会恢复数据库。
- WebHook 匿名入口必须使用短时有效签名、恒定时间比较、请求防重放和敏感值不落日志；触发更新后仍需经过与人工更新相同的配置校验和并发保护。
- 当前更新流程仍是“备份后覆盖”，不是跨多节点原子发布。多实例生产环境启用前，应升级为外部发布系统、分布式锁、健康检查、流量摘除和可验证回滚，不应由单个 Web 进程直接更新整个集群。

## 当前下一步（2026-07-17，系统更新之后）

1. 按旧版菜单盘点 `system/stressTest/index` 压力测试，确认目标来源、请求方式、并发模型、停止机制和结果统计；不新增旧版不存在的能力。
2. 压力测试后端必须限制为超级管理员或系统管理员，并限制目标为明确允许的本机/内网接口、并发数、请求总量、持续时间和请求体大小，防止后台被用作任意地址攻击工具。
3. 自动化验收只检查页面、配置、权限、参数边界和取消链路，不发送真实压力流量；若现有后端无法满足安全边界，页面应保持只读说明或禁用执行，而不是勉强接通。

## 进度追加：接口压测（2026-07-17）

- `develop/stressTest` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保留旧版接口选择、轮数、每轮请求数、并发量、Headers/Body/Path/Query 参数和响应统计功能，没有新增菜单。
- 新版不再解析前端 Swagger 并允许手填任意网址，而是由后端返回核准的只读接口清单；提交时后端再次按“请求方式 + 路由模板”核对，伪造目标地址也不会绕过。
- 服务端会丢弃输入 URL 的主机和端口，统一把请求发送到当前 Admin.NET 后端实例；登录、退出、注册、密码、密钥、支付退款、更新还原、增删改、同步、上传下载、动态查询、外部微信/钉钉和服务器运维等接口不进入清单。
- 安全接口按服务模块分组，默认全部折叠，提供全部展开/折叠和名称/路径搜索；搜索时自动展开命中分组，解决旧版单棵长树难查找的问题。
- 旧版参数上限最高可组合成十亿级请求且并发上限一万，新版后端收紧为每轮 1-500、轮数 1-10、单次总量最多 2000、并发 1-50、最长 30 秒；同一后端实例同时只允许一个压测任务。
- 后端自动沿用当前登录请求的 Authorization，不再让页面填写或回显令牌；Host、Cookie、Authorization、Content-Length、代理认证等敏感/协议请求头禁止由表单设置。
- 并发响应时间统计从非线程安全 `List<double>` 改为 `ConcurrentBag<double>`，请求只读取响应头并及时释放响应，超时会停止剩余任务并在结果中标明。
- `SysCommonService` 不再整类匿名：登录前确需使用的国密密钥对和字符串加密方法单独保留匿名访问；接口清单和压力测试继续要求有效 JWT，服务内部再限制为超级管理员。
- 页面默认仅 1 轮、每轮 10 次、并发 5，执行前必须勾选资源影响确认并输入“压测”；使用说明解释轮数、总请求、并发、QPS、平均值、P99 和安全边界。
- 已通过前端 ESLint、`vue-tsc`、动态路由 10 项单元测试和后端 net8.0 核心编译；浏览器读取到 69 个真实安全接口并完成搜索、选择、配置弹窗和说明验收。自动化验收未执行真实压测，旧版 `Web` 未修改。

## 追加规则：内部压测与主动流量

- 后台压测、批量探测、连通性检查等主动流量功能禁止接受任意协议、主机或端口；目标必须由后端白名单或显式属性核准，并在执行端再次验证，不能只靠前端下拉框。
- “GET/查询/详情”命名不能单独证明接口无副作用。密钥、文件、外部平台、动态查询、服务器信息和高成本元数据接口即使是读取，也默认排除；未来新增可压测接口应采用显式准入评审。
- 请求总量、并发量、单请求参数大小、任务持续时间和同一实例任务数必须在后端设硬上限；前端输入限制只负责体验，不作为安全边界。
- 身份令牌由服务端从当前请求上下文安全转发，不允许浏览器把 Authorization、Cookie、Host 或代理认证作为普通参数保存、显示或批量发送。
- 自动化验收不得为了生成漂亮统计而实际执行压测；只验证接口清单、目标约束、参数上限、确认和取消链路。真实压测必须由用户在独立测试环境、维护窗口内主动执行。

## 当前下一步（2026-07-17，接口压测之后）

1. 按旧版菜单顺序和现有占位清单，优先盘点仍未迁移的只读/低风险页面，先核对旧版确实存在再实施，不新增菜单。
2. 对数据库管理、代码生成、表单设计、审批流等高风险或重组件模块，先完成后端能力、安全边界和第三方依赖审计，再决定原生重写、受控桥接或延后迁移。
3. 继续保持旧版 `Web` 永不修改，只作为功能和交互对照；每个新模块完成后检查真实接口、写副作用、权限、构建、浏览器布局及三端口健康状态。

## 进度追加：库表管理（2026-07-20）

- `develop/database` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保持旧版数据库选择、数据表选择、字段结构、新建/编辑表、新增/编辑/删除字段、生成实体和生成种子功能范围，没有新增菜单。
- 页面默认选中当前配置库和第一张业务表，真实读取数据库元数据；当前库标识为 `1300000000001`，读取到 46 张非 `zero_` 临时表，字段表格固定操作列并使用紧凑布局。
- 新版把只读查看与结构写操作明确分区，顶部持续提示所有结构修改都作用于真实数据库；旧版“删除表”按钮原本禁用，新版也不开放快捷删表入口。
- 新建表弹窗默认只放主键和一个普通字段，可按需加入租户字段、基础字段组并调整顺序；默认数据量下弹窗不出现无意义竖向滚动条，大量字段时仅字段区内部滚动。
- 删除字段要求输入完整字段名，主键字段和数据表最后一个字段由后端禁止删除；编辑字段只允许修改名称和说明，不通过普通表单隐式改变字段类型。
- 实体和种子文件的存放位置只接受后端配置白名单；实体名、表名、字段名和种子后缀执行格式校验，同名代码文件存在时拒绝覆盖。
- 库表服务继续使用 Admin.NET 原有全局 JWT，不增加重复认证中间件；所有库表与代码生成接口额外限制为超级管理员，并校验数据库标识必须来自连接配置白名单。
- 建表、改表、字段变更和种子生成加入单进程写互斥，避免同一实例内并发修改结构或同时写生成文件；数据库类型再次按当前数据库支持清单核对。
- 已通过前端 ESLint、`vue-tsc`、动态路由 11 项单元测试和后端 net8.0 核心编译；浏览器只读验收读取 46 张真实表、16 个示例字段，建表与说明弹窗布局正常且控制台无错误。
- 自动化验收未执行建表、改表、删字段、生成实体或生成种子；旧版 `Web` 目录保持未修改，5005、5666、8888 三个服务继续运行。

## 追加规则：数据库结构与代码文件生成

- 库表管理、数据库迁移、DDL 和代码生成属于开发运维能力，只允许超级管理员；菜单可见和前端按钮禁用不能替代后端身份判断。
- 数据库连接只能从服务端配置白名单选择，禁止页面提交任意连接字符串、主机或账号；表名、字段名、实体名和输出位置必须结构化校验，不能拼接未验证 SQL 或磁盘路径。
- 自动化验收默认只读取库、表、字段和类型清单，并检查弹窗、校验和取消链路；不得创建测试表、删除字段、重命名生产表或生成测试代码文件。
- 删除表、删除列、改类型和批量建表必须先有数据库备份、影响分析和显式确认；数据库 DDL 在不同引擎中的事务能力不同，不能把前端成功提示当成可回滚保证。
- 生成实体和种子文件不得静默覆盖已有文件。生成后必须检查 Git 变更、敏感数据、命名空间和编译结果；种子生成前尤其要排除密码、令牌、证书及个人敏感数据。
- 单进程互斥只能保护当前实例。多实例部署要执行数据库级迁移锁或外部发布锁，不能允许多个节点同时执行结构修改和代码生成。

## 当前下一步（2026-07-20，库表管理之后）

1. 按旧版菜单继续盘点 `system/codeGen/index` 代码生成，核对导入数据表、字段配置、预览、下载压缩包、生成到本地和菜单生成的完整链路。
2. 优先审计代码模板路径、输出目录、覆盖行为、压缩包内容、动态 SQL、生成菜单和数据库写入；生成到本地与生成菜单在自动化验收中不得执行。
3. 新版必须说明每个生成方式的结果和使用步骤，保留旧版真实功能但不扩展为任意模板上传、任意路径写入或任意命令执行。

## 进度追加：代码生成（2026-07-20）

- `develop/codeGen` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保留旧版配置列表、查询、新增、编辑、复制、删除、字段配置、外键/树选择器配置、同步字段、代码预览和正式生成功能范围，没有新增菜单。
- 新版数据库选择、实体选择、后端项目、生成方式、打印模板、控件类型和查询方式均读取 Admin.NET 真实接口或字典；数据库连接串不再返回浏览器，页面只显示“数据库连接已由服务端安全托管”。
- 代码生成所有公开接口额外限制为超级管理员；配置数据库必须来自服务端连接白名单，实体必须真实存在，后端命名空间必须来自 `CodeGen.json` 白名单，实体名和页面目录执行严格格式校验。
- 新增、更新、删除和“同步字段”加入单进程写互斥；主配置与字段配置在同一事务中写入。新版同步接口由服务端重新读取已保存配置，替代旧版前端“先删除整条配置再重新新增”的非事务流程。
- 预览和正式生成只接收记录 Id，后端重新读取可信配置，不再信任浏览器提交的连接串、命名空间、页面目录、菜单父级或输出路径。
- 当前代码模板的前端部分仍是 Element Plus，并且 `FrontRootPath` 指向永久只读的旧版 `Web`。后台和页面双重阻止 `100/111/200/211` 四种包含前端的生成方式，只开放“后端 ZIP（121）”和“后端本地（221）”。
- 后端本地生成会校验目标目录边界并拒绝覆盖任何同名源码；ZIP 只写服务端临时下载目录。迁移期自动生成菜单固定关闭，防止旧组件路径污染 Vben 动态菜单。
- 字段配置更新核对所有 Id 必须属于同一代码生成记录，并校验控件、查询方式、开关、外键字段、字典编码和中文说明，降低模板参数被构造成源码片段的风险。
- 已通过前端 ESLint、`vue-tsc`、动态路由 12 项单元测试和后端 net8.0 完整入口编译。浏览器验收确认真实数据库、实体清单和连接串脱敏；当前数据库代码生成记录为 0 条，自动化验收未新增配置、同步字段、删除记录或正式生成文件。
- 5005、5666、8888 三个服务正常运行；旧版 `Web` 目录保持未修改。

## 追加规则：模板渲染与源码生成

- 预览、生成、同步只能接收持久化记录 Id，服务端必须重新读取并校验可信配置；不得把浏览器提交的连接串、磁盘路径、命名空间、模板名或菜单参数直接用于文件系统和数据库操作。
- 数据库连接串不得出现在列表、详情、下拉选项、日志、截图或前端持久化状态。页面需要解释连接状态时只能返回固定脱敏说明或“已配置/未配置”。
- “同步字段”必须在事务中完成读取、校验、删除旧字段和写入新字段；任何一步失败时原配置保持不变，不能复用前端删除后重新新增的多请求流程。
- 生成到本地必须限制根目录、规范化目标路径并拒绝静默覆盖。自动化验收只允许列表、详情、选项和预览，不得同步、删除、下载生成包、写源码或生成菜单。
- 前端模板必须和目标 UI 框架一致。旧 Element Plus 模板不得写入 Vben 项目，也不得继续写入永久只读的旧版 `Web`；Vben 模板完成并经过编译、权限和 CRUD 验收后才能重新开放前端生成。
- 自动生成菜单会修改权限树和租户授权，必须作为独立高风险能力审计；模板迁移期默认关闭，未来启用时要避免按标题批量删除菜单，并增加事务、唯一标识、影响预览和幂等校验。

## 当前下一步（2026-07-20，代码生成之后）

1. 按旧版菜单继续盘点 `system/formDes/index` 表单设计，先确认其数据保存位置、VForm 版本、导入导出协议、上传组件和运行时渲染依赖。
2. 表单设计属于重组件模块，优先复用成熟设计引擎并保持旧表单 JSON 兼容；不能只做一个好看的空壳，也不能新增旧版不存在的菜单。
3. 自动化验收只检查设计器加载、已有 JSON 导入、预览和取消链路，不保存测试表单、不上传文件；旧版 `Web` 继续永久只读。

## 进度追加：表单设计（2026-07-20）

- `develop/formDes` 已从迁移占位页切换为真实表单设计工作区，没有新增旧版不存在的菜单。
- 经源码核对，旧版页面只嵌入 `vform3-builds 3.0.10`，没有表单保存、列表、发布或版本管理后端接口；新版保持相同边界，不伪造 CRUD，也不让用户误以为离开页面后设计会自动保存。
- 新版继续使用与旧版一致的 VForm `3.0.10` 和 Element Plus `2.8.8`，保证旧版导出的表单 JSON 可以导入，新版导出的 JSON 也能回到旧版使用。
- VForm 和 Element Plus 只随 `system/formDes/index` 路由懒加载；普通 Ant Design Vue 页面不主动加载设计器。生产构建已确认生成独立 `formDes`/`designer` 资源，不进入首屏主包。
- 页面采用独立全宽工作区，隐藏 VForm 已失效的外部链接和重复品牌栏，保留组件库、模板、PC/Pad/H5、撤销重做、清空、预览、导入导出 JSON、导出代码和生成 SFC 等原生能力。
- 页面补充说明弹窗，明确“当前无服务端保存接口”和“离开前必须导出 JSON”，同时提醒只导入可信 JSON。
- 已通过 ESLint、`vue-tsc`、生产构建和动态路由 13 项单元测试；浏览器确认 Element Plus 组件已真实解析，预览弹窗和说明弹窗均可正常打开关闭。
- 自动化验收未导入外部 JSON、未上传文件、未保存测试表单；旧版 `Web` 保持未修改。

## 追加规则：低代码表单与可执行配置

- VForm 表单 JSON 不是普通静态数据：自定义函数、校验规则和事件脚本可能被执行。任何后续“服务端保存、模板共享、导入市场、跨租户复制”都必须按可执行代码处理，增加来源信任、权限、审计、内容检查和租户隔离，不能直接把陌生 JSON 交给设计器或渲染器执行。
- 为保持旧版表单兼容，本阶段不擅自重写 VForm JSON 协议，也不删除旧版已有的脚本能力；页面必须明确提示只导入可信文件。生产环境若启用严格 CSP，需要单独验证 VForm 的动态脚本能力，不能为了通过部署而放宽整个后台的 CSP。
- Element Plus 和 VForm 属于受控桥接依赖，只能在专用重组件路由中加载。迁移普通 CRUD 页面时继续使用 Ant Design Vue，禁止因为表单设计器存在就重新把 Element Plus 扩散到全局业务组件。
- 表单设计器当前没有后端持久化能力。后续如需保存、发布、版本、回滚、权限和运行时渲染，应作为独立业务模块设计数据库和接口，经数据模型与安全评审后实施，不能复用字典、参数配置等无版本表临时存放。

## 当前下一步（2026-07-20，表单设计之后）

1. 继续核对旧版已有的 `approvalFlow/index` 审批流程模块，先盘点实际页面、插件后端、LogicFlow/VForm 依赖、流程定义保存位置和运行时审批接口，再决定原生重写还是受控桥接。
2. 审批流程属于可改变业务状态的高风险模块。自动化验收只允许读取流程列表、查看设计器和取消弹窗，不新建流程、不发布流程、不发起审批、不执行同意/驳回/撤回。
3. 若旧版菜单只是入口而本地插件、数据库表或接口不完整，则保留清晰的只读说明或占位状态，不新增不存在的流程产品能力，也不修改永久只读的旧版 `Web`。

## 进度追加：审批流程定义（2026-07-20）

- `platform/approvalFlow` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保留旧版流程定义列表、新增、编辑、删除、业务表绑定和 LogicFlow 流程图设计入口，没有新增旧版不存在的菜单。
- 经插件源码核对，当前 Admin.NET 审批插件只有“流程定义存储”能力，没有待办列表、发起审批、同意、驳回、撤回、审批实例和节点执行引擎。新版页面明确标注“定义中心”，删除旧版固定演示时间线，不把演示数据包装成真实审批记录。
- 审批定义表没有 `TenantId` 字段，属于全局数据。所有公开接口除沿用 Admin.NET 全局 JWT 外，服务层再次限制为超级管理员，防止普通租户读取或修改全局流程定义；本轮没有增加重复认证中间件，也没有修改应用启动入口。
- 新增和更新改为显式 DTO，禁止浏览器覆盖创建人、修改人和软删除字段；流程编号按日期与最大序号生成，并增加单进程写互斥和名称/编号唯一性校验。
- 表单绑定 JSON 限制为对象、16 KB、合法数据库配置标识和允许操作白名单；流程图 JSON 限制为 1 MB、最多 500 个节点和 1000 条边，并校验允许的节点与连线类型。配置 JSON 只保存，不作为服务器端脚本执行。
- LogicFlow 设计器采用路由级懒加载，支持开始、审批、条件、结束节点，兼容旧版常见节点名称，并提供撤销、重做、缩放、适配画布和只读 JSON 查看。只有已有流程记录才能保存设计结果。
- 已通过定向 ESLint、`vue-tsc`、生产构建、动态路由 14 项单元测试和后端 net8.0 完整入口编译；浏览器只读验收确认页面、使用说明、新增弹窗和空状态正常，分页组件没有产生新告警。
- 当前数据库流程定义为 0 条。自动化验收未新增、编辑、删除、绑定业务表或保存流程图；因此行级详情、绑定和设计器保存链路保留为“等待真实记录后人工验收”，不能标记成已执行写入验收。
- 旧版 `Web` 目录保持永久只读，5005、5666、8888 三个服务继续运行。

## 追加规则：流程定义与运行时能力边界

- “能画流程图、能保存定义”不等于“审批系统已可运行”。页面、文档和验收结论必须分别说明定义配置、审批实例、任务流转、消息通知、权限代理和审计留痕是否真实存在，禁止用静态时间线或前端模拟状态补齐后端缺失能力。
- 流程定义、表单配置和条件表达式属于可执行或准可执行配置。后端必须限制大小、结构、节点类型、目标表和操作白名单；未来若增加表达式或脚本，必须单独进行沙箱、超时、租户隔离和审计评审。
- 没有 `TenantId` 的业务配置表默认按全局敏感数据处理。除非完成数据库模型和历史数据迁移，不得只在前端加租户筛选并宣称已实现租户隔离。
- 自动化浏览器验收只允许查看列表、说明、设计器和取消链路，不创建流程、不保存图、不绑定真实业务表、不发布流程，也不执行任何同意、驳回、撤回或业务表写操作。

## 当前下一步（2026-07-20，审批流程定义之后）

1. 重新读取旧版动态菜单和旧版 `Web` 页面目录，优先选择仍存在的低风险模块；不根据占位清单自行新增菜单。
2. 对 `mqttx/index`、`elive/index`、`home/notice/index` 和 `system/onlineUser/index` 逐项核对旧版页面、真实后端接口、外部服务依赖和菜单可见性，再确定下一项。
3. MQTT、实时通信和音视频模块的自动化验收默认不连接外部代理、不发布消息、不推流、不踢人，只检查只读状态、配置边界、取消链路和错误提示。

## 进度追加：站内信收件箱（2026-07-20）

- `dashboard/notice` 已从迁移占位页切换为 Vben/Ant Design Vue 真实页面，保持旧版标题、类型查询、分页、阅读状态和详情功能，没有新增菜单。
- 页面接入真实 `/sysNotice/pageReceived` 与 `/sysNotice/setRead` 接口；列表只返回当前登录账号的接收记录，打开未读消息时才标记已读，查询、翻页和查看已读消息不会产生写入。
- 通知正文不再像旧版一样直接把数据库 HTML 交给 `v-html`，而是通过 DOMPurify 清洗后展示；列表摘要也从清洗后的纯文本生成，降低管理员富文本造成存储型脚本执行的风险。
- 表格按窗口宽度自适应：窄屏保留序号、标题、类型、阅读状态和查看操作，宽屏再显示内容摘要、发布者和时间，避免字段硬挤和操作列遮挡。
- 已通过定向 ESLint、`vue-tsc`、15 项动态路由单元测试和生产构建。浏览器只读验收读取到当前账号 1 条真实已读通知并打开详情，页面没有新告警。
- 自动化验收没有打开未读消息，因此没有调用标记已读写接口；旧版 `Web` 目录保持永久只读。

## 追加规则：消息内容与已读副作用

- 通知、公告、邮件模板和富文本备注即使只由管理员编辑，也必须按不可信 HTML 处理；显示前进行成熟清洗，禁止直接渲染脚本、事件属性、危险协议和可执行嵌入内容。
- “查看详情”若同时具有标记已读副作用，页面必须明确说明；自动化验收优先使用已读记录，不能为了验证弹窗擅自改变用户未读状态。
- 接收列表必须由后端按当前用户限制数据范围，前端隐藏其他用户记录不能代替服务端条件。标记已读也必须同时按通知标识与当前用户标识更新。

## 当前下一步（2026-07-20，站内信之后）

1. 核对旧版在线用户是否仅作为顶部抽屉而非独立菜单；若新版顶部在线用户面板已经覆盖真实功能，则补齐验收与路由边界，不重复创建第二个页面。
2. 之后审计旧版 `mqttx/index`：先确认它是否为纯前端 MQTT 客户端、连接参数保存位置、默认代理地址、认证信息、TLS、订阅和发布能力，再决定受控迁移方式。
3. `elive/index` 旧版含硬编码的萤石演示令牌与设备地址，迁移时禁止复制这些凭据；必须改为由用户会话输入或安全后端配置，并默认不自动播放外部视频。

## 进度追加：在线用户与真实菜单复核（2026-07-20）

- 旧版 `system/onlineUser/index` 不是数据库动态菜单页面，而是顶部工具栏中的在线用户抽屉；新版 `online-user-panel.vue` 已覆盖真实会话列表、租户/账号/姓名筛选、当前窗口识别、同账号其他窗口识别和强制下线，因此不重复新增菜单。
- 在线用户的一条记录代表一条 SignalR 浏览器连接，同一账号在多个浏览器、窗口或标签页登录时出现多条记录是正常行为；新版用“当前窗口”“同账号其他窗口”“其他账号”标签解释差异，避免用户误认为账号重复。
- 强制下线只允许系统管理员及以上且具有 `sysOnlineUser:forceOffline` 权限的账号执行；当前窗口不提供自我下线按钮，其他会话下线前显示明确影响说明。自动化验收只读取列表，没有下线任何真实会话。
- 已只读查询当前 MySQL 菜单数据：`mqttx/index`、`elive/index`、`system/onlineUser/index` 均不在启用菜单中，不能因为旧源码目录或演示页面存在就迁移并新增菜单。
- 当前启用的内部菜单路由共 37 个，已逐页等待动态路由和主内容加载后做只读冒烟检查；没有迁移占位页、404 或空白页。行政区划中的 `140400` 是行政区代码，不是 404 错误。
- 菜单中的框架教程、Furion、Element Plus、SqlSugar 等外部链接继续按数据库原配置展示；`develop/api` 保持后端文档 iframe 入口，不擅自改写数据库菜单地址。

## 进度追加：关于页面与外链安全（2026-07-20）

- `about/index` 已从通用占位映射切换为 Admin.NET Vben 专用关于页面，展示 Admin.NET、.NET 8、Vben 5.7、Ant Design Vue、MySQL 和动态权限等当前项目真实技术信息，没有新增菜单。
- 动态菜单外链现在只接受完整的 HTTP/HTTPS 地址，拒绝 `javascript:` 等可执行协议，也拒绝在 URL 中携带用户名和密码；非法地址不会被转换为可点击路由。
- 菜单新增/编辑表单同步提供中文校验，iframe 菜单必须填写合法外链；后端 `SysMenuService` 再次执行同样的协议与凭据校验，避免绕过前端直接写入危险链接。
- 本次后端修改沿用 Admin.NET 原有全局 JWT、菜单按钮权限和服务校验，没有新增重复鉴权中间件，也没有修改应用启动入口。
- 已通过 19 项动态路由单元测试、前端 ESLint、`vue-tsc`、生产构建和后端 net8.0 编译；后端重启后 5005、新版 5666、旧版 8888 均正常监听。

## 追加规则：动态菜单与外部地址

- 数据库中不存在的菜单不得根据源码目录、演示页面或 Vben 示例自行补建；必须先确认旧版真实菜单、后端接口和业务用途。
- 外链与 iframe 地址必须在前端和后端同时限制为 HTTP/HTTPS，并拒绝嵌入账号密码、脚本协议和无法解析的地址。前端隐藏或禁用不能替代服务端落库校验。
- 菜单读取和全路由冒烟验收只能读取当前配置，不自动修复、同步、重排或重写菜单表；发现失效外链时先记录影响，再由用户确认是否修改真实数据。
- 在线用户、消息中心等顶部全局能力若已完整覆盖旧版交互，不再创建重复业务页面或菜单；但真实接口、权限边界和异常状态仍需单独验收。

## 进度追加：分页与列表交互统一（2026-07-20）

- 新版业务列表统一使用 `ADMIN_PAGINATION_PROPS`：支持每页 10/20/50/100 条、页码快速跳转、每页条数切换、窄屏响应式收敛和较少页码显示。
- 已扫描所有业务页面，原有分页列表均已接入统一配置；最后一个独立写法的顶部在线用户抽屉也已合并，不再出现行政区划有跳页框、其他页面没有的差异。
- 树、详情子表和固定少量数据表可明确使用 `pagination=false`，不为了视觉一致强行增加无意义分页；是否分页由数据规模和真实后端协议决定。

## 当前下一步（2026-07-20，全菜单冒烟之后）

1. 进入跨模块收口阶段：统一查询区、表格操作列、详情弹层、空状态和危险操作确认，不再迁移数据库菜单之外的演示页面。
2. 优先检查所有分页接口在切换页码、每页条数和筛选后是否正确重置页码，并核对后端返回的 `total/items/page/pageSize`，避免只有组件外观统一、实际翻页仍错误。
3. 继续执行权限与数据安全回归：超级管理员、系统管理员、普通账号分别验证可见菜单和接口边界；自动化不执行同步、删除、生成、压测、支付、发布、审批或数据库结构写入。
4. 保持旧版 `Web` 永久只读，仅用于功能和交互对照；每轮结束检查 `git diff -- Web` 必须为空。

## 进度追加：分页查询行为收口（2026-07-22）

- 三套服务已重新后台启动并完成健康检查：后端 `5005`、新版 `5666`、旧版 `8888` 均返回 HTTP 200；启动继续使用项目内 Node 22，不修改系统全局 Node。
- 真实浏览器验收发现操作日志位于第 2 页时点击“查询”仍停留第 2 页，筛选后可能跳过前一页结果。该问题属于分页状态与查询条件没有正确分离，不是单纯的分页组件样式问题。
- 已统一参数配置、AD 域配置、任务调度、文件管理、打印模板、行政区域、微信支付、操作日志、访问日志、异常日志、差异日志共 11 个页面：点击查询、回车搜索或切换租户时先回到第 1 页，再调用真实分页接口；点击页码时仍按用户选择的页码加载。
- 全局分页配置补齐窄屏响应式和精简页码显示；顶部在线用户抽屉也接入同一套每页条数、快速跳页和响应式规则。固定少量子表仍明确关闭分页，不增加无意义控件。
- 已扫描新版全部查询按钮，不再存在分页查询按钮直接调用 `loadRecords/loadJobs/loadRegions` 的遗留写法；菜单树、机构树、职位和注册方案等非分页列表不强行套用该规则。
- 已通过定向 ESLint 和 `vue-tsc`。浏览器真实验证操作日志第 2 页首条序号为 51，点击查询后当前页回到 1、首条序号回到 1；没有执行日志清空、导出或其他写操作。

## 追加规则：查询条件与分页状态

- “查询、回车搜索、重置、切换租户或其他数据范围”属于新的结果集合，必须先把当前页重置为 1；“点击页码、上一页、下一页”才允许保留用户选择的页码。
- 查询按钮不得直接复用只负责请求的 `load*` 函数。页面应使用独立的 `handleQuery/search` 入口重置分页，再调用加载函数，避免以后新增筛选条件时再次遗漏。
- 分页样式统一不等于分页正确。每轮验收至少核对第一页与第二页首条数据、每页条数、总数、筛选后页码以及空结果，确认前端参数和后端返回契约一致。
- 自动化分页验收只能执行读取、查询和翻页；不得顺带点击同页的清空、删除、同步、生成、压测、支付、发布或审批按钮。

## 当前下一步（2026-07-22，分页收口之后）

1. 执行跨模块权限回归：先用无令牌请求确认关键管理接口统一返回 401，再核对高风险接口是否有超级管理员、菜单按钮和数据范围校验。
2. 重点检查租户、菜单、角色、用户、任务调度、数据库、代码生成、系统更新、压测和支付接口；不重复添加认证中间件，不修改 Admin.NET 应用启动入口。
3. 继续只读验收页面布局、查询和详情；任何真实写入必须先说明数据影响并由用户主动执行。

## 进度追加：账号与角色权限边界收口（2026-07-22）

- 已确认 Admin.NET 的真实鉴权链路位于 `Admin.NET.Web.Core/Startup.cs` 与 `JwtHandler`：全局 JWT 负责登录状态，`JwtHandler` 把接口路径映射为按钮权限，例如 `/api/sysRole/grantMenu` 对应 `sysRole:grantMenu`；超级管理员按框架原规则跳过按钮校验。未新增第二套认证中间件，也未修改应用启动入口。
- 任务调度、系统更新等高风险服务中的“系统管理员或超级管理员”检查属于业务风险的第二道边界，不替代按钮权限。普通 CRUD 继续使用菜单按钮权限、租户过滤和数据范围，不统一粗暴改成“只有超级管理员”。
- 只读回归确认 Admin.NET 未登录时使用 HTTP 200 包装业务码 `401`，不能只看 HTTP 状态判断鉴权结果。系统管理员可读取本租户 4 个普通账号，普通账号 `user1` 只读取到自己 1 个账号和自己 1 个角色，说明租户与机构数据范围正常生效。
- 修复角色授权接口过去直接信任浏览器角色/菜单 Id 的边界：更新、删除、授权菜单、授权数据、状态修改和授权详情读取都会先校验目标角色是否属于当前租户及当前账号可管理范围。
- 角色菜单授权同时校验菜单必须真实属于目标租户；普通账号只能把自己拥有的菜单授权给自己拥有或创建的角色，不能通过伪造菜单 Id 扩大权限。新增角色改为事务操作，菜单校验失败时不会留下半条角色数据。
- 修复账号管理的目标对象边界：更新、删除、状态、授权角色、重置密码、解除锁定、角色详情和附属机构详情都会按账号类型、租户和机构数据范围校验目标用户，不能通过伪造用户 Id 读取或操作列表范围外账号。
- 新增/编辑账号时，主机构、附属机构、职位和角色都必须真实存在于目标租户；普通账号只能分配自身数据范围内机构，以及自己拥有或创建的角色。超级管理员也不能通过普通账号接口再创建一个超级管理员账号。
- 修复编辑账号后“机构或角色变化检测”时机错误：现在先记录原机构和原角色，再写入新值；发生变化时才强制旧会话下线，确保新权限即时生效。
- 新版账号弹窗只对超级管理员显示“系统管理员”账号类型，始终不显示“超级管理员”创建项；角色和职位按表单目标租户过滤。浏览器验收默认租户显示 4 个可分配角色，没有跨租户混入。
- 已通过账号页 ESLint、完整 `vue-tsc`、后端核心与完整入口 net8.0 编译。接口只读验收确认：读取本人角色返回 200，伪造其他角色返回 `D1016`，读取数据范围外用户返回 `D1013`，读取超级管理员目标返回 `D1038`；未执行新增、编辑、删除、授权或状态修改。
- 后端编译仍提示项目现有依赖安全公告，其中 `System.Linq.Dynamic.Core 1.5.1` 为高危。该依赖升级需要独立做动态表达式兼容回归，本轮不和权限修改混合处理。

## 追加规则：按钮权限、数据范围与服务保护

- 权限验收必须同时检查四层：登录状态、按钮权限、租户隔离、数据范围/目标对象。页面上看不到按钮不能证明接口安全，接口返回 200 也不能证明业务授权成功。
- 所有接收用户 Id、角色 Id、菜单 Id、机构 Id、租户 Id 的写接口，都必须在服务端重新查询并校验目标对象；禁止信任前端下拉框、树组件和隐藏字段已经限制了范围。
- 授权集合必须满足“目标存在、属于同一租户、操作者有权授予”三个条件。授权失败必须在事务中回滚，不能先删除旧授权、再因新授权校验失败留下空权限或半成品。
- 超级管理员、系统管理员和普通账号是不同业务身份。普通 CRUD 不得为了省事全部限制为超级管理员；系统更新、数据库结构、代码生成、压测、全局调度等高风险能力可以增加服务级管理员保护，并保留独立按钮权限。
- Admin.NET 统一结果会把未登录包装成 HTTP 200 + 业务码 401。自动化和前端请求层必须检查业务码，不能只检查 HTTP 200；后端原生启动和鉴权管线保持不改。
- 权限回归默认使用读取接口和拒绝路径，不执行真实新增、授权、删除、同步或状态修改。需要验证成功写链路时，必须先准备隔离测试租户和可清理测试数据，再由用户明确允许。

## 当前下一步（2026-07-22，权限边界收口之后）

1. 继续跨模块页面收口：扫描查询区、表格操作列、修改记录弹层、详情弹层和空状态，优先修复仍然使用黑色默认气泡、操作遮挡、无意义滚动条或尺寸不一致的页面。
2. 页面统一只抽取稳定的展示组件和样式，不把各模块真实业务表单强行做成同一尺寸；短表单使用紧凑弹窗，长表单按分组和内容高度决定弹窗或抽屉。
3. 完成只读浏览器巡检、前端类型检查、后端健康检查和 `git diff -- Web` 零改动检查；旧版继续永久只读。

## 进度追加：修改记录弹层一致性（2026-07-22）

- 全量扫描新版业务页面后，发现菜单管理仍使用深色 `Tooltip` 承载创建者、创建时间、修改者、修改时间和备注，属于唯一一个把复杂详情塞进提示气泡的遗留实现。
- 菜单管理已改为与机构、角色等页面一致的白色 `Popover` 信息弹层，使用紧凑描述表格、轻边框和受控阴影；弹层位于修改记录列下方，不再用黑色气泡覆盖相邻操作按钮。
- 普通图标和短文案说明继续使用 `Tooltip`；多字段详情统一使用 `Popover` 或详情弹窗，避免再次出现黑底、难读和层级遮挡。

## 进度追加：请求日志敏感信息收口（2026-07-22）

- 最终巡检发现 Furion `LoggingMonitor` 会把完整请求头输出到控制台；本地后台启动脚本又会把控制台重定向到文件，因此登录令牌可能被写入 `.run-logs/backend-5005.log`。
- 修复位于 Admin.NET 原有 `LoggingSetup`：控制台只过滤 `System.Logging.LoggingMonitor` 的完整请求快照，普通启动日志、业务异常和其他日志继续保留。没有修改 JWT、`UseAuthentication`、`UseAuthorization`、`JwtHandler` 或接口返回结构。
- 数据库操作日志仍由原有 `DatabaseLoggingWriter` 保存业务字段，日志详情读取继续经过 `LogSanitizer`；本次没有为了脱敏关闭数据库审计能力。
- 后端重新编译并启动后，用已登录页面触发真实读取请求；新日志中原始 JWT、`Authorization`、Cookie 和请求监控整包计数均为 `0`，5005 接口保持正常。
- 旧版 `Web` 未修改，新版请求层无需变化；该修复同时保护 8888 和 5666 访问同一后端时产生的本地日志。

## 追加规则：认证信息与运行日志

- 任何环境的控制台、文件、数据库、异常邮件和前端错误上报都不得保存完整密码、JWT、刷新令牌、Cookie、签名密钥或数据库连接串；排查问题只记录固定掩码、摘要或与密钥无关的请求标识。
- 日志脱敏必须发生在写入前；详情页读取时再次脱敏属于纵深保护，不能代替源头过滤。已经生成的本地日志发现凭据后应及时清理或轮换，禁止把日志提交到 Git。
- 过滤请求监控日志不能通过关闭认证、修改请求头或绕过权限实现。认证与授权继续沿用 Admin.NET 原生管线，日志配置只负责输出边界。
- 验收日志安全时只输出命中数量和布尔结果，不在终端、截图、计划文档或聊天中回显真实令牌。

## 当前下一步（2026-07-22，权限与页面收口之后）

1. 独立审计现有依赖安全公告，优先核对 `System.Linq.Dynamic.Core 1.5.1` 的高危影响范围，再评估 `log4net 3.0.3` 与 `MailKit 4.9.0` 的兼容升级路径。
2. 升级前先扫描动态排序、动态查询、邮件发送和日志写入的真实调用点，建立针对性编译与接口回归；不得只为消除警告盲目抬版本。
3. 继续保持三服务可用、旧版 `Web` 永久只读；自动化只执行编译、只读查询和拒绝路径，不发送邮件、不修改业务数据。

## 进度追加：后端依赖漏洞收口（2026-07-22）

- 使用 .NET/NuGet 原生漏洞审计复核完整后端入口，原状态包含 `AngleSharp`、`log4net`、`MailKit`、`System.Linq.Dynamic.Core` 4 个直接漏洞包，以及 MessagePack、MimeKit、ImageSharp、SQLite 原生库和 XML 加密库等传递漏洞。
- 直接依赖保持原主版本升级：`AngleSharp 1.5.2`、`log4net 3.3.2`、`MailKit 4.17.0`、`System.Linq.Dynamic.Core 1.7.3`；同步满足 MailKit 的 `BouncyCastle.Cryptography 2.6.2` 约束。
- 传递依赖采用兼容补丁线收口：ASP.NET Core 三个组件统一到 `8.0.29`，MessagePack 固定为 `2.5.302`，ImageSharp 固定为 `3.1.12`、Web 组件为 `3.1.5`，XML 加密库为 `8.0.4`，SqlSugar 为 `5.1.4.216`，SQLite 原生库为 `2.1.12`。
- `Admin.NET.Core` 与完整 `Admin.NET.Web.Entry` 的 net8.0 还原、编译均通过；两级 `--vulnerable --include-transitive` 审计均返回“没有易受攻击的包”。
- 运行回归确认 MySQL 正常、任务调度读取到 5 个真实作业、操作日志分页正常、SignalR 在线用户面板显示当前连接；后端日志没有 JWT、认证头、Cookie、MySQL 并发错误或启动异常。
- 未自动发送真实邮件、上传或处理真实图片，也未连接 SQLite；这些链路会产生外部副作用或当前生产配置不使用，保留为上线前的受控人工验收项。

## 追加规则：依赖安全升级

- 依赖安全审计必须同时覆盖直接依赖和传递依赖，并以实际解析版本为准；只修改项目文件但没有成功还原、编译和重新审计，不能标记完成。
- 优先在现有主版本和目标框架补丁线内升级。出现包降级时按上游最低版本约束处理，禁止关闭 `NU1605` 或强行忽略还原错误。
- 核心依赖升级后至少回归其真实使用面：动态表达式对应排序/校验，SignalR 对应在线连接，SqlSugar 对应当前 MySQL 查询，日志库对应启动与错误输出。邮件、文件、图片等有副作用能力只做编译和配置检查，真实操作由人工在隔离数据下验收。
- 漏洞清单归零不代表功能回归完成；上线前仍需在目标服务器确认 .NET 运行时补丁、数据库驱动、Redis/SignalR 集群和外部邮件服务的环境兼容性。

## 当前下一步（2026-07-22，依赖安全之后）

1. 执行发布候选收口：前端生产构建、动态路由单元测试、类型检查、后端完整编译、三服务健康检查和旧版 `Web` 零改动检查。
2. 整理仍需人工执行的写链路验收清单，使用隔离测试租户覆盖用户/角色/菜单授权、租户切换、上传、邮件、打印、代码生成和流程定义；不得在现有业务数据上自动造数。
3. 发布前建立 Git 回滚点并核对 `_app.config.js`、Nginx/API/SignalR 代理、MySQL 备份与敏感配置注入；未完成环境验收前不替换旧版生产入口。

## 进度追加：发布候选自动化检查（2026-07-22）

- 新版前端 `vue-tsc` 类型检查通过，动态菜单适配器 19 项单元测试全部通过，Vite 生产构建成功并生成 `dist.zip`。
- 完整构建实际解析了 VForm、LogicFlow、打印设计器、ECharts 等重组件；普通业务页面、流程设计与表单设计均形成独立路由资源，没有把所有重依赖合并进首屏。
- 构建仍提示 VForm 3.0.10 与 `vue-plugin-hiprint` 内部使用 `eval`，并且表单/打印设计器单包超过 2 MB。这是旧版兼容桥接的已知发布风险：继续保持按需加载，生产 CSP 与性能验收必须单独覆盖，不能静默忽略。
- 后端完整入口 net8.0 编译通过，三服务 `5005/5666/8888` 均返回 HTTP 200；旧版 `Web` 目录保持零改动。
- 浏览器只读回归已覆盖登录态恢复、菜单、任务调度、操作日志和在线用户实时连接，没有执行新增、删除、授权、同步、邮件、上传、打印或流程保存。

## 发布前人工写链路清单

- 准备独立测试租户、测试机构、测试角色和测试账号，并先备份 MySQL；所有新增、编辑、删除、授权和状态切换只操作带统一测试前缀的数据。
- 验证账号、角色、菜单和租户的新增、编辑、授权、切换、重置密码与删除回收，重点确认失败事务不会留下重复权限、空授权或半成品数据。
- 使用隔离文件验证上传、下载、预览和删除；使用测试邮箱验证 SMTP，不向真实用户发送通知。
- 使用测试模板验证打印、代码生成、表单 JSON 和流程定义；生成源码不得写入旧版 `Web`，不得覆盖已有业务文件，流程只保存测试定义、不发起真实审批。
- 验证 `_app.config.js`、Nginx 的 `/api` 与 SignalR 代理、上传大小、超时、Cookie/Token 头转发、HTTPS 和缓存策略；完成后再建立 Git 回滚点和部署版本号。

## 进度追加：界面结构与交互规范收口（2026-07-22）

- 修复 VForm 3 与全局 SVG 样式冲突：组件库图标和中文名称恢复为同一行，左侧字段不再只显示半截；同时收窄设计器两侧面板并保持独立滚动，桌面宽度下左、中、右三个工作区完整可见。
- 修复顶部多页签滚动实现：滚动视口与内容层重新分离，左右按钮改为可访问的图标按钮，并由真实 `scrollLeft/clientWidth/scrollWidth` 更新边界状态。浏览器实测可从末端回到 `0`，也可从起点滚到末端。
- 异常日志、访问日志、任务调度、微信支付和审批流程的查询区改为稳定宽度与受控换行；查询与重置保持标准按钮宽度，不再让输入框或按钮无意义占满整行。
- 账号新增、编辑、复制统一从右侧抽屉改为 `880px` 居中弹窗；菜单新增/编辑收敛为 `720px` 紧凑弹窗。表单间距、分组和固定底部操作区已统一，内容能放下时不显示竖向滚动条。
- 全量修复新版业务页面中 Ant Design Vue 弹窗数字宽度误传为字符串的问题。固定宽度统一使用数值属性，避免浏览器得到无效的 `width: 720` 后回退成接近全屏的大弹窗。
- 动态菜单增加只读前端图标兜底：按真实路由为账号、角色、机构、日志、任务、表单等菜单补图标，不修改 `SysMenu` 数据库字段，也不改变菜单权限和排序。
- 工作台删除 Vben 演示项目、假天气、假待办和假动态，改为读取 Admin.NET 真实账号数、角色数、机构节点、在线连接和站内信；快捷入口只显示当前动态路由中真实存在且当前账号可访问的模块。
- 浏览器只读验收确认真实工作台显示 5 个账号、4 个角色、5 个机构节点、1 个在线连接和当前站内信；未执行新增、编辑、删除、清空、授权、同步或其他写操作。

## 追加规则：后台页面密度与弹窗尺寸

- 查询区使用内容决定宽度：普通文本框建议 `150-220px`，日期范围建议 `280-320px`，复杂关键字最多约 `360px`；桌面端优先紧凑横排并受控换行，禁止查询或重置按钮单独拉伸占满一行。
- 固定数字宽度的 Ant Design Vue 弹窗必须使用数值绑定，例如 `:width="720"`；不得写成 `width="720"`。弹窗同时保留 `max-width` 的窄屏约束。
- 短表单使用居中弹窗，底部操作区固定；只有内容确实超过当前视口时才允许正文滚动。不要通过固定过小的 `max-height` 给本来能放下的表单制造竖向滚动条。
- Element Plus、VForm、打印和流程设计等重组件样式必须限制在专用页面根节点内；发现全局样式冲突时做局部适配，禁止把 Element Plus 样式重新扩散到普通 Ant Design Vue 页面。
- 菜单缺图标优先使用前端路由兜底，不为了视觉效果写数据库；工作台和统计页面只展示真实接口数据，接口不可用时显示 `--` 或空状态，禁止用演示数据伪装系统状态。
- 旧版 `Web` 继续永久只读。本轮 `git diff -- Web` 为空；后续所有界面重构仍以旧版功能为参照，但不得修改旧版文件。

## 当前下一步（2026-07-22，界面规范收口之后）

1. 按旧版功能清单继续逐页巡检剩余详情弹层、操作列、空状态和查询区，优先处理仍存在黑色复杂气泡、横向操作遮挡或尺寸异常的页面。
2. 使用隔离测试租户执行人工 CRUD 与授权写链路，不在当前默认数据上自动造数；界面验收与写接口验收分开记录。
3. 发布前保留当前 Git 回滚点，继续检查重组件 CSP、首屏性能、生产代理和 SignalR；旧版入口在新前端写链路完全验收前保持可回退。

## 进度追加：发布候选执行基线（2026-07-28）

- 数据库当前启用的 37 个内部路由均已有真实页面或明确的能力边界；工作台、全菜单图标、紧凑查询区、统一弹窗和多页签滚动已完成只读验收。
- 动态菜单图标继续采用前端路由兜底，菜单适配器现有 22 项单元测试；不修改 `SysMenu` 图标、权限和排序数据。
- 第 5、6、7 阶段的总控状态已按实际实现校正。Office 预览和 MQTT 因当前数据库没有独立启用入口而不擅自新增菜单；导入导出继续作为跨页面能力验收。
- 本地运行目录、日志目录、Codex 构建目录和 MySQL 备份目录必须排除在 Git 之外；敏感配置只通过本地配置或环境变量注入。
- 当前顺序固定为：发布候选自动检查 -> Git 回滚点 -> MySQL 备份 -> 隔离测试租户 CRUD/授权写链路 -> 生产代理、CSP、性能与部署边界验收。
- 旧版 `E:\admin.NET\Web` 永久只读，只用于功能对照和生产回退；任何新功能、样式或兼容修复都不得修改旧版文件。

## 进度追加：发布候选检查与工具链收口（2026-07-28）

- 动态菜单适配器 22 项测试全部通过，`vue-tsc` 类型检查、完整 `pnpm lint`、Vite 生产构建和 Admin.NET net8.0 完整入口编译均通过。
- 生产构建已重新生成 `apps/web-antd/dist.zip`；VForm 3 和 `vue-plugin-hiprint` 的内部 `eval`、表单/打印设计器大包仍作为已知 CSP 与性能风险保留，不以关闭构建警告的方式掩盖。
- pnpm 工作区已把迁移新增依赖统一改为 catalog 引用，并删除精简 Vben 应用后无人引用的目录残留；没有升级当前业务依赖版本。
- Oxfmt 与 ESLint 原有两条 Vue 模板纯换行规则会互相改写同一批页面。现由 Oxfmt 单一负责模板排版，ESLint 继续负责 Vue、TypeScript、依赖和质量规则；完整检查已通过。
- 三服务健康检查结果：后端 `5005`、新版 `5666`、旧版 `8888` 均监听并返回 HTTP 200；`git diff -- Web` 仍为空。
- 变更文件敏感信息扫描未发现硬编码运行凭据；命中项仅为连接串脱敏、置空和原有备份命令说明。数据库配置、`.env`、运行日志、便携运行时和备份文件均不进入 Git。

## 进度追加：租户写链路与数据完整性收口（2026-07-28）

- 发布候选数据库备份已生成到 `E:\admin.NET\.backups\AdminNET-20260728-121617.sql`，大小约 `52.39 MB`，SHA256 为 `AF32F91229D3E93AB9BB1958C96BA5A2FC85A1EFC74415FA12F01793A6161372`；备份目录已排除 Git。
- 使用独立测试前缀和隔离租户执行真实 API 链路：超级管理员登录、租户新增/编辑、租户菜单去重授权、同步授权、切换租户、进入租管端、机构新增/编辑、角色新增/授权/编辑、账号新增/角色绑定/编辑、账号/角色/机构删除、测试租户删除。完整链路连续通过两次。
- 验收不是只看页面提示。清理后数据库复核：`SysMenu` 保持 `180` 条，测试租户/机构/角色/账号残留为 `0`，租户菜单与角色菜单重复为 `0`，租户菜单、角色菜单、用户角色、角色机构孤儿关系为 `0`，测试 Logo 文件残留为 `0`。
- 修复 Admin.NET 原租户删除逻辑的危险范围：删除租户只清理目标租户的账号、角色、机构、职位和关系数据，不再把全局 `SysMenu` 当租户数据删除；关系主键在主表删除前采集，避免留下角色菜单等孤儿记录。
- 租户菜单授权会先校验租户和菜单真实存在，对菜单 Id 去重；`SysTenantMenu` 增加 `(TenantId, MenuId)` 唯一索引声明，使新数据库环境也具备重复写入保护。
- 同步授权改为合并租户现有菜单与租户角色实际菜单；默认租户额外合并当前数据库全部菜单，兼容插件动态菜单和旧 `sys_admin` 角色迁移，不再把新租户管理员权限错误缩减到基础角色范围。
- 租户 Logo 只允许 PNG/JPG/JPEG、最大 `2 MB`，文件名会清理路径字符；新增租户不再把 DTO 中的默认公共 Logo 当作旧上传文件删除，删除租户时也只允许清理受管上传目录内的 Logo。
- 真实写链路验收脚本位于本地忽略目录 `.codex`，不保存密码，不进入 Git。运行时凭据只能通过当前会话环境变量注入。

## 追加规则：写接口与数据完整性

- 每个新增、编辑、授权、同步、切换和删除功能都必须同时验收页面交互、接口业务码、数据库关系和清理结果；“按钮提示成功”不能作为完成依据。
- 授权或同步接口完成后必须检查唯一键重复、关系孤儿、越租户引用和权限数量异常；任何可能全量重建关系表的逻辑都要先备份，并在隔离租户中验证幂等性。
- 删除聚合对象时先读取关联主键，再按目标租户或目标对象精确删除关系表，最后删除主表；禁止用全局菜单、角色或账号集合推导删除范围。
- 自动化写验收只允许操作本次生成且带唯一测试前缀的数据；失败时只清理能证明属于本次任务的数据，禁止用模糊名称或全表条件做补偿删除。
- 写链路结束后必须复核核心主表数量、重复关系、孤儿关系、上传残留和旧版目录差异；发现异常先停止后续写入并从备份或 Git 回滚点恢复。

## 进度追加：生产部署边界收口（2026-07-28）

- 生产构建使用同源 `VITE_GLOB_API_URL=/api`，动态 `_app-config-*.js` 仅包含 API 相对地址；构建产物中没有 `localhost:5005/5666/8888`、数据库连接串或数据库密码。
- 登录页默认账号和密码现在只读取 `.env.development`。生产构建不再预填或打包开发默认登录凭据。
- `scripts/deploy/nginx.conf` 已补齐 `/api/`、`/hubs/`、`/upload/` 三类代理，SignalR 转发 WebSocket Upgrade 头；`index.html` 和动态配置禁用缓存，带哈希静态资源使用长期缓存，移除原模板的全开放 CORS。
- Dockerfile 已从真实的 `apps/web-antd/dist` 复制产物，不再引用不存在的 `playground/dist`；容器通过 `ADMINNET_BACKEND` 注入后端地址，模板不会把本地端口写入前端包。
- 完整部署说明见 `docs/adminnet/production-deployment.md`。Docker 客户端存在，但本机 Docker Desktop 引擎当前未运行，因此容器启动与 Nginx 容器内语法检查保留到预发布环境执行。
- VForm 3 和 `vue-plugin-hiprint` 的构建资源仍包含动态代码执行能力。为保持旧版表单 JSON 和打印模板兼容，本轮不强行删除；它们继续按路由懒加载，也不为了通过检查给整个后台自动添加宽松 CSP。

## 当前下一步（2026-07-28，发布候选技术收口之后）

1. 在预发布域名部署当前发布候选，验证 HTTPS、Nginx、动态配置、API、SignalR、上传、刷新路由和缓存更新；旧版生产入口继续保留。
2. 使用隔离测试数据人工验收菜单、字典、职位、文件、打印、代码生成、表单 JSON 和流程定义，逐项记录数据库与文件副作用，不发送真实短信、邮件或通知。
3. 完成 MySQL 恢复演练、服务器 .NET/Node/Nginx 版本核对和回退演练后，再建立正式发布版本并安排新版入口切换。

## 进度追加：核心 CRUD 与导出能力收口（2026-07-29）

- 后端 `5005`、新版 `5666`、旧版 `8888` 已重新启动并保持 HTTP 200；旧版 `Web` 目录没有任何差异。
- 新增可复用验收脚本 `scripts/validation/validate-adminnet-core-crud.mjs`，密码只从 `ADMINNET_TEST_PASSWORD` 环境变量读取，不写入仓库、命令默认值或日志。
- 脚本已真实完成菜单按钮、机构、职位及复制、角色菜单/数据范围授权、字典类型、字典值及复制、字典缓存的新增、读取、编辑和删除；所有测试记录按反向依赖顺序清理。
- 清理后数据库复核：全局菜单保持 180 条，本轮测试菜单、机构、职位、角色、字典类型和值均为 0；租户菜单、角色菜单重复为 0，租户菜单、角色菜单、角色机构孤儿关系均为 0。
- 修复字典值后端两个原有问题：分页的显示文本筛选改为查询 `Label`，状态切换使用真实 `DictTypeId` 清理缓存。浏览器在字典值中搜索“会员”准确返回 1 条。
- 参数配置补齐旧版真实存在的表格导出，支持导出本页和按当前查询条件导出全部；敏感参数只导出后端已经脱敏的值，不从其他接口绕过保护。最终采用 Excel 可直接打开的 UTF-8 CSV，避免引入内置旧版 SheetJS 代码的依赖，并增加公式注入防护。
- 请求信封、Token 响应头和菜单适配器测试已覆盖成功、异常、大小写兼容、失效清理和菜单边界；核心后端 net8.0 编译、Vben 类型检查均通过。
- 打印模板已使用临时 A4 空白模板完成新增、更新、分页回读和删除；列表与数据库均无 `ARTIFACT_` 测试残留。
- 审批流程已完成定义新增/更新、业务表绑定、开始到结束流程图保存、详情回读和软删除；验收后按精确 Id 清理软删除审计行，最终残留为 0，没有发起真实审批。
- 代码生成已使用当前数据库中未配置且无同名生成物的实体，完成配置新增/更新、字段配置保存、源码预览、后端 ZIP 生成与下载；配置、字段、目录和 ZIP 均清理，字段孤儿为 0。生成方式固定为后端 ZIP，没有写入旧版前端或已有后端源码。
- 表单设计器在浏览器完整加载 VForm 3，组件库、PC/Pad/H5、预览、JSON 导入/导出、代码导出和 SFC 入口正常；实际点击 JSON 导出已触发下载。该模块与旧版一致没有服务端保存接口，因此不会产生数据库记录。

## 当前下一步（2026-07-29，功能收口）

1. [x] 使用统一测试前缀和隔离模板验收打印文件、代码生成后端 ZIP、表单 JSON 导出与流程定义保存和删除回收；生成文件没有进入或覆盖旧版 `Web`。
2. [x] 外部依赖模块按安全边界收口：短信、邮件、LDAP、微信、MQTT、Office 等没有真实测试配置时不发送、不连接，也不伪造“已实测”结论；后续拿到测试凭据后另建专项验收记录。
3. [x] 当前发现的界面和功能问题均按“旧版功能对照 -> 真实接口 -> 数据库/文件副作用 -> 清理 -> 页面体验”的顺序闭环；后续继续沿用该规则，不再用仅看页面或仅看 HTTP 200 代替验收。

## 最终本地验收（2026-07-29）

- 三套服务保持运行：后端 `5005`、旧版 `8888`、新版 `5666` 均返回 HTTP 200。
- 旧版 `E:\admin.NET\Web` 的 Git 差异为 0，整个功能补齐过程只把旧版当作运行对照。
- Vben 的 ESLint、TypeScript 类型检查、生产构建均通过；生产压缩包约 3.41 MB。
- Admin.NET `net8.0` 后端编译通过，0 个错误；仍有 3 条上游包针对 `net6.0` 目标的既有兼容性警告。
- 请求、Token 和菜单适配器共 28 项单元测试全部通过。
- 参数导出已在浏览器再次实际验收：导出菜单正常打开，导出本页 15 条成功，菜单自动关闭。
- 新增的 Excel 依赖在提交前被移除，最终采用零依赖 CSV；仓库依赖审计仍报告 71 项既有告警，主要来自 Vben 构建工具和重型旧组件，作为后续依赖升级专项处理，不与本次功能迁移混为“已消除”。
- 当前结论：数据库已启用且旧版实际存在的内部管理功能，迁移主体和核心真实写链路已完成；需要第三方账号、密钥或真实外部环境的功能只完成界面、接口和安全边界接入，不能称为真实联调完成。
